# Freight workspace completion

User objective: `.codex/attachments/38716b06-3475-4178-8ace-87a663210c27/goal-objective.md` in the user's Codex directory. The full scope is all eight clashes plus operational depth across Sea, Air, Road and Rail, with a target of at least 95% supported by evidence. Percentages will not substitute for acceptance criteria.

## Invariants

Keep mutable autosaved drafts and immutable submitted Quote versions; master Quote references and Booking references; manual and secure-link acceptance; latest-link validity; selective accepted-version Booking updates; mode-change confirmation; Booking-only changes never modifying Quotes; PDF and operational audit history; branch-relative directions; separate planned, estimated and actual dates. Tracking connection remains deferred until Multideck Live/Sinay is ready. AI actions require approval and use existing permissions.

## Delivery and evidence ledger

| Workstream | Required outcome | Evidence gate | State |
| --- | --- | --- | --- |
| Deployment | Explain failure, restore affected deployment without altering team setup | Vercel failure logs, successful matching build and route smoke test | Investigating |
| Shared policy | Consistent mode/direction/service/stage choices, field visibility and validation | Behaviour tests across supported combinations and browser checks | In progress |
| Booking Details | Control, Parties, Route & schedule, Cargo & equipment sections with persistent job context | Keyboard, narrow/wide and reduced-motion browser checks | Implemented locally; wide browser checked; narrow/keyboard pending |
| Cargo | Editable multiple lines, dimensions, dangerous goods and equipment allocation | Save/reload, totals, conversion, revised Quote and isolation tests | Multi-line editor and stable item save implemented locally; deeper records/allocation pending |
| Quote model | Structured cargo/equipment retained through snapshots, PDFs and Booking updates | Round-trip and immutable historical-version tests | Typed version cargo, editor/payload round-trip, PDF projection, readiness, initial and selective revised cargo handover tested locally; public response lifecycle, legacy mapping and full live verification pending |
| Typed operational data | Route, equipment and cargo fields use existing typed structures | API/database round-trip, permissions and audit checks | Shipment goods value separated and tested locally; wider operational fields and live round-trips pending |
| Submitted Quotes | Readable immutable summary with revision action | Version switching and customer/internal visibility tests | Implemented locally, including saved charges and missing-snapshot protection; focused render/state and isolated browser checks passed; hosted lifecycle pending |
| Sea | FCL/LCL, containers, seals, VGM, cut-offs, documents, milestones across directions | Representative import/export/cross-trade flows | Pending |
| Air | AWB, flight legs, ULD, dimensions, chargeable weight, screening, milestones | Representative Air flow without Sea-only fields | Pending |
| Road | Stops, vehicle/trailer/driver, appointments, CMR/POD | Domestic and cross-border flows | Pending |
| Rail and multimodal | Rail references/equipment and per-leg mixed-mode policy | Rail and mixed-mode save/reload flows | Pending |
| Dexter | Read, approved writes and event-driven Watching parity for changed backend capabilities | Matching/non-matching/pause/resume and permission tests | Exact cargo-line read/edit/watch implemented and tested locally; live and broader parity pending |
| End-to-end | Quote draft/send/respond/PDF/Booking/revision/notification lifecycle | Real browser/API/database evidence, controlled test recipients only | Pending |
| Release | Reviewed commits, live schema/function parity, deployment smoke tests | Exact commit and deployed artefact evidence; migration reconciliation | Pending |

## Deployment evidence, 5 September 2026

- Project `multideck-app-dev`, team Databrain Solutions.
- Commit `72a6b6c` succeeded on `dev`: deployment `dpl_AmyJD4yzQa2GAsp5nHzwanJam3b8`.
- The same commit failed on `codex/Prefix-update`: `dpl_694HJVrNTpRs3e38Ss2VNZihJdwF`.
- Failure is explicit: `Multideck App build blocked: MULTIDECK_SURFACE is required on Vercel.` It occurs in the product-context prebuild guard, before TypeScript/Vite compilation.
- Do not remove this guard, copy tenant credentials into source, or alter team settings to hide this error. Verify branch-specific environment availability. No Vercel settings have been changed.

## Current checkpoint

Base `72a6b6c`, recovery tags retained. Implementation branch `codex/freight-workspace-foundation`. Existing integration report records 72 passing focused tests and 55 broad-suite failures already present in parent branches. Those are baseline evidence, not proof of this goal's completion.

## Foundation checkpoint, 5 September 2026

- Shared mode/service policy now drives the active Quote Details and Booking Details views. Rail and specialist modes no longer fall through to Road in register mapping. Sea/rail containers follow the service and actual legs, not just Import/Export. ULD remains a valid Air shipment choice. This is presentation policy; server-side policy and specialist route validation still require reconciliation.
- Shared package vocabulary extracted without changing existing choices. Booking cargo package type now uses it too.
- Booking Details has four context-preserving sections. Multi-line cargo list supports selecting, adding and removing a line with confirmation. Parent draft state keeps edits when changing sections; blank descriptions are blocked before save.
- Source ID and Last updated are factual read-only values. VIN is now cargo data, not a transport vehicle registration. Legacy VIN values that were actually vehicle registrations are not guessed or migrated.
- Found and corrected a save-function identity risk: ordinary saves formerly soft-deleted every cargo/container and inserted replacements. New migration `20260905110317_booking_stable_cargo_equipment_identity.sql` keeps supplied active IDs, checks job ownership, rejects duplicates/stale identities, safely renumbers cargo, preserves unexposed typed fields and JSON, and only soft-retires omitted records. Accepted Quote replacement without item IDs deliberately creates new rows while keeping old history.
- The previous save function body was compared with `pg_get_functiondef` from development project `aqtwypsuijxlnvtxpuxe` and matched before the patch. No live DDL or tenant data changes have been made for this checkpoint.
- Real PostgreSQL regression test creates its own disposable local cluster, executes the migration function and verifies repeated saves, line reordering, foreign job IDs, stale IDs, duplicate IDs, required description, atomic failure, DG/seal links, retained dimensions/VGM/JSON, new rows, soft retirement and audit. Surrounding permission/workspace functions are isolated test stubs: this is not a full RLS, tenancy or end-to-end deployment test. It skips explicitly where PostgreSQL binaries are unavailable.
- Browser proof: added an unsaved second cargo line, edited its description, switched to Route and back, selected the original line and confirmed each description remained independent, then discarded test edits. No live Booking save or customer email was sent.
- Local production build passed. Focused suite: 49 passing tests, including real PostgreSQL behaviour; tests that demanded first-line-only editing and duplicate policy dictionaries were updated to the intended behaviour. Shared Tabs motion scan: zero findings. Responsive/keyboard/reduced-motion runtime checks remain pending.
- Repository code for Dexter `update_booking` has a separate older mutation path with only a first-cargo summary. A subsequent read-only live check found that function is absent from development, and its booking action registry currently lists only complete, reopen and send-to-customs. This is concrete deployment-parity drift, not just a UI limitation. Do not claim cargo-line editing or full validation parity through Dexter yet. Add the approved action through the canonical booking save boundary, registry and watches before release. Existing save audit events are retained; browser roles are explicitly denied direct execution of the privileged save function.
- Preview environment repair awaits the user's scope choice; no Vercel project, environment or team setup was changed.

Next: finish stable cargo round-trips and Dexter parity, structured Quote cargo snapshots and summaries, then mode-specific operational records. Do not deploy this migration/UI checkpoint as a completed all-mode platform or report the 95% target achieved.

## Dexter cargo verification checkpoint, 5 September 2026

- Resumed the isolated cargo draft from safety commit `74dca12` on the implementation branch after the source-control checkpoint. This work is not yet pushed or deployed.
- Added an explicit `booking_cargo` read domain and one-field approved edit action using the canonical Booking save and measurements boundaries. Reads contain exact line/Booking identities and update evidence, not prices, margins or raw commercial JSON. Ordinary saves retain other lines and declared values; numeric values, units, safety flags and nullable clears are validated.
- Approval is mandatory in both chat modes and in the database registry. Discovered and corrected the executor's cargo-specific conflict between a newly resolved target and a subsequently recorded explicit approval. Company, intent, session grant, current permissions and stale Booking checks remain enforced. The executor's unrelated action behavior is unchanged.
- The Booking audit records the exact before/after field and operator reason. Replaying a successful prepared action returns its previous result without a second Booking mutation.
- Cargo watches use exact line IDs and notify only. The evaluator emits each distinct changed event, remains quiet on no-op/nonmatching changes, respects pause/resume, and rechecks active ownership and Booking permission. Restrictive owner-history access and the privileged list wrapper also enforce current Booking access without replacing the existing private-support or CRM guards.
- Local PostgreSQL tests execute the actual save, measurements, registered domain dispatch, action, prepared-action executor, approval trigger, watch creation/evaluation, notification insertion and owner RLS. They cover foreign lines/actors/workspaces, invalid and financial fields, stale approvals, dimensions/clears, preserved values, repeated changes, retries, permission loss and history isolation. Auth/permission resolution, unrelated product domains and the broad workspace read use declared isolated fixtures; these are not full live tenant, browser/AI or full RLS deployment tests.
- JavaScript behavior tests exercise mandatory approval, operator edit versus read intent, and exact identity storage in a prepared (not executed) action. Existing Dexter/security contracts are also rerun.
- Focused result: 24 tests passed with no skips. No Deno executable is available in this checkout environment, so a full Edge Function type-check is not claimed.
- Still required before release: full live migration reconciliation, schema advisors, actual user-scoped Dexter chat and watch round trips, and the broader cargo allocation/DG/Quote model work. Adding/removing cargo, financial edits and detailed DG/equipment allocation are explicitly unsupported by this one-field Dexter action until their canonical workflows are completed.
- Vercel read-only verification: merged `dev` commit `390de336ae6003228fe91ffce4c63218331be621` is READY as `dpl_7k1PA1WhsHX2EURAiciDMCK6vCvT`. This proves successful deployment of the previous integrated client checkpoint, not the new local cargo action or database migration. Earlier feature-branch environment failure remains distinct; no team/project configuration was changed.

## Structured Quote cargo foundation, 5 September 2026

- Added an unapplied migration defining `quote_api.version_cargo_lines`: typed quantities, weights, volume, dimensions, package type, commodity, origin, HS code and basic safety flags, keyed by Quote version and stable cargo-line ID. Commercial values and detailed DG/allocation records are not silently folded into this shape.
- The existing version snapshot remains immutable evidence. Triggers validate/project structured `quote.shipmentFacts.cargoLines` atomically. Draft saves keep the existing version ID, update surviving line IDs in place, remove only omitted draft lines, and cascade only the legacy helper's transient draft version. Submitted versions retain their original snapshot and lines.
- Incomplete descriptions can autosave but cannot cross the submitted-version boundary. Numeric, flag, identifier, duplicate, unit, country-code and unsupported-field errors reject the whole save. Legacy flat snapshots are not rewritten or split into guessed allocations.
- The table has RLS, no browser grants and no independent application-role write permission. Only internal version triggers write its projection; service-role read access is private and is not an authorisation boundary for a future API.
- Real disposable PostgreSQL test executes the new migration, existing immutable-version guard and current customer-identity/draft-collapse function. It covers repeat saves, reordering, linked-record preservation, a revised version, immutable history, incomplete draft/submission, invalid-save atomicity and role denial. The older broad Quote save is an explicit fixture; full tenant save, browser, send, conversion and live advisors remain unverified.
- Focused verification: 14 tests passed, zero failed, zero skipped, combining the new Quote PostgreSQL lifecycle with Booking cargo approval, Booking/PostgreSQL watch lifecycle and Dexter security tests. No client UI changed in this checkpoint; no fresh browser or client-build proof is claimed.
- **Not a release-ready structured cargo feature yet:** Quote UI/payload mapping, header/snapshot consistency, pre-send readiness, PDF/public response rendering, initial Booking conversion and accepted-revision comparison/apply must all use the same lines before enabling it. Current Booking revision conversion still builds one summary cargo row. Do not deploy or expose this partial feature as complete.
- Explicit temporary Dexter exception: individual Quote cargo read/edit/watch is unsupported until canonical line-level handover and permission-safe adapters are connected. The prompt now states this limit rather than treating a Quote summary as line-level evidence. Existing Quote lifecycle and Booking cargo capabilities are unchanged; no new recurring AI evaluation was added.
- Additional evidence found while tracing documents: `quotePdfDataset` currently prefers today's payer CRM terms over the version's saved terms. Historical PDF rendering must be corrected to use issued-version commercial evidence, with a regression test for later payer changes. No terms correction is claimed in this checkpoint.
- Next work remains structured cargo throughout the editor, snapshots, document/readiness and Booking paths, followed by operational mode depth and controlled live parity. No percentage or overall completion claim is justified by this foundation test.

## Version-bound PDF and cargo output, 5 September 2026

- Corrected the payer-terms drift above. The production PDF dataset builder now uses the selected version snapshot for terms, conditions, customer notes, parties, validity, shipment mode/service and route details. It no longer queries today's payer CRM profile or fills historical gaps from the current master header. Blank historical terms remain explicitly unrecorded rather than gaining new conditions at render time. Send-recipient overrides remain delivery metadata and do not replace the saved payer/customer on the PDF.
- Structured cargo gets an explicitly customer-safe projection and compact repeating table in the existing Carbone template. All supported lines, dimensions, package types, weights, HS/origin and basic safety flags survive; internal notes/cost fields are never spread into the output. Legacy flat cargo remains a single labelled summary, not invented allocations.
- Displayed cargo totals come from the version's lines using exact decimal addition, not stale flat summary fields. A missing line measurement leaves that total unknown. An explicit single routing leg is now honoured, and the previous 30-leg render truncation was removed. Source braces and HTML are escaped so user text cannot become another local template substitution.
- Behavioral tests execute the actual dataset builder/formatters and HTML renderer, with only company branding/storage reads stubbed. They prove later-header isolation, blank/missing historical values, 500 cargo lines, accurate zero/decimal totals, partial measurements, preserved source data, internal-charge exclusion and literal template-like user text. Combined focused document suite: 18 passed, zero failed, zero skipped.
- Deno is now available through `npx deno`. `deno check --node-modules-dir=none --no-lock` passed for the full `quotes-workflow/index.ts` import graph, the cargo projection and PDF renderer. This supersedes the earlier tool-availability limitation for this checked graph only; it is not proof of every Edge Function.
- Local visual QA used the real HTML renderer and isolated Chrome/Chromium printing with CSS page size. The short fixture is one A4 page; 70 lines with a long description occupy six A4 pages. Representative first/middle/final pages were inspected; all 70 descriptions occur exactly once in extracted PDF text and continuation pages repeat column headings. Synthetic artifacts are at `/tmp/multideck-quote-document-fJ6D63`; no customer records, email or hosted Carbone call was involved.
- This extends the existing customer-facing, tenant-branded quote output without changing the internal app theme or introducing a new gallery component. The existing quote delivery/document events and watch permissions are unchanged; no new chat send authority or recurring AI watch has been introduced. Individual Quote cargo read/edit/watch remains the explicit temporary unsupported exception recorded above.
- Still not release-ready: Quote editor/payload persistence, pre-send readiness, public response presentation, initial and revised Booking handover, actual hosted Carbone rendering/email/storage, live migration parity and end-to-end browser flows remain required. The broader all-mode completion goal remains active.

## Structured cargo pre-send readiness, 5 September 2026

- New unapplied migration `20260905123223_quote_cargo_issue_readiness.sql` integrates every structured cargo line into the existing server-side readiness function. Missing descriptions, quantities, package types and mode-relevant measurements identify the affected line. Non-container quantities and relevant weights/volumes must be positive to count as complete; drafts still allow incomplete and zero values to autosave. FCL does not gain invented per-container goods allocations.
- Normalised header cargo must match the current saved version. Missing versions, stale summary data, malformed cargo and an explicit empty list cannot bypass the structured check. The legacy flat flow remains unchanged when neither side contains structured cargo. Readiness does not rewrite either record.
- Existing customer, Incoterm/N/A scope, named-place, routing, container and charge rules remain in the same function. The migration fails for review if its integration anchors have unexpectedly changed. Service-only access and the existing caller/company guard are retained.
- A real disposable PostgreSQL test executes the actual cargo migration, current readiness function, new migration and `quote_workflow_prepare_customer_response_v4`. It proves an incomplete second line blocks before any response link is created, correction permits a version-bound pending link, mismatched cargo blocks, N/A/FOB and charge rules remain, and a foreign-office attempt is denied without a new link. It also checks Air/Courier/Sea/Ocean/Road/Rail measurement alternatives, malformed/zero values and function privileges. The permission resolver and minimal table schema are explicit fixtures, not a complete tenant or provider test.
- Combined focused result: 26 tests passed, zero failed or skipped, including both real Quote/PostgreSQL tests, document behaviour and existing sync/container contracts. No product UI changed; a new browser or client-build result is not claimed for this SQL-only change.
- Dexter's existing Quote domain description now explains line-aware issue readiness. No new write or watch action was added: existing send permissions and deterministic Quote events remain the boundary. Individual Quote cargo read/edit/watch is still the explicit unsupported exception until its canonical adapters are connected.
- Remaining critical path: editor/payload round-trip; initial Booking conversion and accepted-revision comparison/apply still use single-row cargo logic; typed operational precision/source identity; public response presentation; hosted document/email/storage verification; live migration reconciliation. This does not complete the structured cargo feature or the broader all-mode goal. No live schema or customer data was changed.

## Initial accepted Quote cargo handover, 5 September 2026

- Added unapplied `20260905123929_quote_booking_cargo_handover.sql`. Initial conversion now creates one Booking cargo row per structured accepted-version line. Typed descriptions, commodity, quantities, package type, weights, volume, dimensions, HS/origin and basic safety flags are copied without inventing allocations. Chargeable weight remains in the existing Booking cargo JSON compatibility field; its operational typed migration is not complete.
- Each new cargo row has a composite foreign key to the exact version/cargo source, an indexed provenance lookup and a unique active source-line constraint within its Booking. Ordinary canonical Booking saves preserve these unexposed identifiers. The insertion helper is internal-only, verifies the exact submitted/accepted source against the Booking, and refuses to replace existing cargo. It is not used by accepted-revision application.
- Existing typed Booking columns support two decimal places for weights/dimensions and six for volume, with bounded commodity/package/HS strings. Readiness now reports values which would be rounded or truncated, while incomplete drafts remain saveable. The conversion repeats this compatibility check atomically. No global column widening, silent rounding or shortening was introduced.
- One structured cargo line inherits the shipment goods value; multiple lines keep individual declared values blank, retaining the unallocated shipment value in the accepted source snapshot. No equal split or repeated shipment total is invented. Legacy flat Quote conversion is unchanged.
- Expanded real PostgreSQL test executes the actual original initial conversion body under its current renamed boundary, the new integration, typed source projection, and canonical stable Booking save. It proves two-line conversion, measurements/flags/source identities, operator save preservation, repeat-conversion idempotence, Quote evidence remaining unchanged, wrong-version/duplicate insertion refusal, foreign-key integrity, atomic rollback for incompatible values, and legacy/single-line declared-value behaviour. Reference allocation/numbering, broad workspace response and permission resolver use explicit fixtures; the outer revision/routing/payer/container wrappers and live provider lifecycle are not claimed as exercised by this database fixture.
- Combined focused result: 17 passed, zero failed/skipped, including three real PostgreSQL tests and existing revision/container contracts; the expanded single-line case also passed a final focused rerun. No UI or live deployment was performed.
- Existing Booking creation/save events and cargo read/edit/watch permissions remain the integration route; no new public action or event type and no recurring AI polling were added. Per-Quote cargo actions remain the previously documented unsupported exception until canonical adapters are complete.
- **Do not expose/deploy the partial structured Quote feature yet.** Accepted-revision comparison/apply still needs per-line ownership/conflict handling. The Booking goods editor currently falls back to the shipment-wide goods value on each selected line; that must be separated from line allocations before enabling structured cargo. Remaining work also includes editor/payload mapping, public presentation, operational typed measurements, live parity and end-to-end evidence. All eight original goal areas remain in scope.

## Cargo revision comparison and planning, 5 September 2026

- Added unapplied `20260905125327_quote_cargo_revision_comparison.sql`: internal deterministic three-way comparison by stable Quote cargo ID, not row position. It compares previous Quote, current Quote-owned Booking cargo and the newer Quote. Booking-only changes and reordering do not create differences. New/removal/restoration operations are explicit; ordinary changes are individually selectable fields, including clears and safety flags.
- A private planner binds the Booking's applied version and latest accepted submitted Quote to the same master, locks the Booking in canonical order and validates unique selections. It rejects changed selected values/operations since the observed review. Each planned update includes complete current values, preserving unselected Booking edits; operational-only cargo is excluded, never a removal target. Long operational descriptions remain intact even beyond the Quote editor's text limit.
- These functions perform **comparison/planning only**. No public/service-role execution grants and no new product read/write/watch capability are exposed. The existing apply RPC still uses its legacy summary path; do not wire the new difference keys into that path without the persistence and audit work below. The explicit temporary Dexter Quote cargo exception remains authoritative.
- Expanded disposable PostgreSQL fixture proves conflicting description versus safe weight update, explicit additions/removals/restorations, field clears, reordering, Booking-only changes, operator-added cargo protection, per-field and apply-all plans, stale baseline/selected-field rejection, refresh recovery, unique/allowlisted selections, unaccepted version refusal and no mutation during planning. Combined focused verification: 11 passed, zero failed/skipped, including three real PostgreSQL tests and existing sync contracts.
- Next integration points are identified: `public.booking_workflow_apply_quote_sync_before_payer_20260904` is the underlying apply body; its 30-field cap, single `cargo` payload and applied-fields/audit tracking need coordinated per-line handling. Public review reads currently return stored differences, so stale observations also need a refreshed review/token flow. The Edge Function's `parseQuoteSyncFields` allowlist and the Booking selection UI must accept validated dynamic cargo keys only after that guarded path exists. Preserve existing mode confirmation, payer/container/routing wrappers and financial boundaries.
- Still pending: actual selective revision persistence, original/applied per-field provenance in audit, full public review/Apply flow, legacy-to-structured cargo mapping, shipment-versus-line values, editor/public response work and live release gates. No completion percentage, live migration, deployed feature or finished revision-update claim is made.

## Guarded selective cargo persistence, 5 September 2026

- Added unapplied `20260905130449_quote_cargo_revision_persistence.sql`. The internal write boundary checks the active caller's Booking permission, company/office, exact Booking/review and pending version before regenerating the plan from authoritative versions. It never accepts a client-supplied mutation plan. It writes only selected typed/JSON fields; unselected operational values, formatting, declared values, source identity and linked records remain untouched.
- Additions/restorations get an individual cargo row and accepted-version source link. Removals soft-archive the selected row, preserving linked DG/history. Per-field decisions and before/after cargo evidence, review ID, baseline/proposed version IDs and actor are recorded in existing Booking audit events. Original source-version columns remain creation provenance; later field application evidence is in the version-bound audit.
- Partial application records applied keys without advancing the Booking's applied master version. Full completion advances it only when every review difference has been applied. Retried already-applied keys return a receipt without overwriting subsequent Booking edits, creating duplicate audit events or emitting duplicate cargo signals.
- Corrected the planner's acceptance binding: a later draft/revised master or cleared convenience accepted-ID must not invalidate the version already accepted and waiting for Booking review. The source version must be submitted and accepted, with no newer accepted version for that master. Tests prove a later working draft permits the pending application and a newer acceptance refuses it.
- Expanded real PostgreSQL test exercises partial/completed writes, cross-workspace and unauthorised denials, financial-key refusal, stale-review rejection, retry idempotence, old Quote preservation, operator-only cargo protection and retained DG foreign-key history. It also installs the actual existing Dexter cargo event trigger: a matching write emits one source/company-bound signal, and retry emits none. Downstream notification evaluation remains covered separately by the existing Booking/Dexter lifecycle fixture; this is not a full chat/browser/provider test.
- Focused result: 17 tests passed, zero failed/skipped. The first test run exposed an ambiguous variable in the test assertion, which was corrected; the production function executed successfully. All checks are local. No migration, UI or provider deployment was performed.
- **Still internal and not release-ready:** no role (including service role) has direct execution access to the new helper. Public review creation/refresh, dynamic field-key validation, mixed cargo/non-cargo Apply coordination and UI observations still need connection to this boundary; the old summary apply path is not made safe merely by this helper existing. Preserve mode confirmation and existing payer/routing/container wrappers during that integration. Dexter cannot invoke the unwired helper; the documented Quote cargo exception remains in force. The rest of the original all-mode objective remains unfinished.

## Refreshed cargo review and mixed Apply integration, 5 September 2026

- Resumed the unverified recovery draft from `48cec3b` after the remote safety checkpoint `129b687`. New unapplied migration `20260905132442_quote_cargo_review_integration.sql` extends the existing projection/difference chain, returns a fresh server-generated review token, and coordinates cargo and standard field application in one transaction. Original Booking-at-review evidence is retained; applied field receipts remain visible after refresh. Token differences are deterministically ordered, and Quote reference/version labels are preserved.
- The public v2 boundary checks active caller, Booking permission, current office/company, exact master/review/baseline/pending version and latest submitted accepted source. It rejects changed review observations, unavailable selections and unconfirmed mode changes. Completed retries return receipts without overwriting later operational edits. The old summary path rejects dynamic cargo keys and cannot collapse structured cargo to update shipment goods value.
- `bookings-workflow` now targets the v2 read/apply RPCs, validates bounded allowlisted cargo field keys and the review token, and returns a recoverable 409 on stale observations. The Booking API and existing review panel carry that token, identify the affected cargo, preserve individual/all-available selection, show unmapped changes explicitly, and provide Refresh review. A pre-existing parent callback discarded the mode confirmation boolean; it now forwards it. Partial state and success copy no longer falsely claim an unchanged Booking or an exact match after retained operational edits.
- Real disposable PostgreSQL evidence now installs the existing projection, routing, payer, container and canonical save/apply functions around the new boundary. Tests cover mixed cargo/customer-note application, a genuine standard-save failure rolling back cargo/receipts/audit/watch signals, stale-read refusal and refresh, partial/completed application, confirmed mode changes, idempotent retry, original evidence preservation, old-path refusal and cross-workspace/function-grant checks. Identity permissions and the broad document workspace remain declared fixture boundaries; the review row is seeded, so initial review creation is not yet end-to-end evidence. Focused result: 17 passed, zero failed/skipped.
- Seven Deno boundary tests passed and the entire Booking Edge Function import graph type-checked. Production client build passed with existing bundle warnings; the final focus adjustment also passed TypeScript checking. Existing repository-wide failure limitations remain.
- Isolated Chrome QA uses the actual review-panel source and shared primitives with synthetic callbacks, not a real Booking or Supabase session. It verified blocked-row semantics, Apply available/mode confirmation, stale error/no-success, refresh/token/confirmation delivery, busy controls, a visible partial receipt, 375px and 320px reflow, both supported English number-format contexts, and reduced motion. No browser errors were reported. Modal cancellation now restores the invoking button; while applying it returns to the review heading instead of losing focus. The motion scanner reports zero findings. No new reusable component or tenant-branded surface was introduced: this remains the existing Multideck-owned Booking review.
- **Release gate remains closed.** Deploy the migration before this Edge Function/client pair; never fall back to old unguarded Apply when v2 is missing. Live schema parity, actual browser/API/database round-trip, new-review creation, all standard wrapper branches, hosted documents and providers still need verification. Legacy-to-structured mapping and shipment-versus-line values are explicitly unavailable, not silently guessed. Whole-line add/remove detail presentation also needs richer disclosure before broad structured-cargo rollout. No database or provider deployment was performed.
- Dexter continues to read Booking review availability and cargo evidence through existing tenant-safe domains; cargo writes emit the existing deterministic signals. Applying accepted Quote revisions remains explicitly routed to the Booking review rather than a new autonomous Dexter action. The prior individual Quote cargo read/edit/watch exception remains until its canonical adapters are connected. This is progress on the original goal, not an assertion of 95% completion.

## Cargo value separation and complete line disclosure, 5 September 2026

- Removed the Booking editor's shipment-value/currency fallback from individual cargo values. Existing code already protected explicit nulls and non-first lines; the remaining defect affected a first line with an absent property. The field now reads only the selected line and is labelled Cargo line value. Saved allocations and explicit zero/clears remain unchanged, and no-line views cannot create a value-only cargo row. The accepted snapshot's shipment value is separately labelled, read-only evidence above the cargo list. This is not yet an editable typed Booking shipment total or completed allocation reconciliation.
- Whole-line accepted-Quote additions/removals/restorations now offer an expandable three-way comparison of all 16 supported cargo fields. Missing lines, unrecorded fields, false flags and zero measurements remain distinct. Descriptions wrap in full and numeric comparisons no longer round to three decimal places. The disclosure is outside the selection label, so inspecting a line does not approve it. Addition warnings no longer claim that the Booking was changed. Only the explicit cargo field allowlist is rendered, never arbitrary JSON or internal notes.
- Five behaviour tests execute the actual client mapping/formatting expressions; the eight existing review contracts also pass (13 total, no failures/skips). The production client build passed before the final warning-copy adjustment, with existing bundle warnings. Narrow/wide isolated browser QA used the actual review panel and shared primitives: disclosure opening, Enter/Space, unchanged checkbox selection, full description, six-decimal volume, 320px reflow, UK/US English contexts and internal-field exclusion passed; no browser errors were reported. This does not prove actual tenant/API persistence. The added shipment-value summary was checked through source mapping/build, not a signed-in browser flow.
- Final TypeScript checking passed after the warning-copy adjustment. These are existing Multideck-owned panels and controls; no new reusable component, tenant theme, backend capability or permission was introduced. Existing Dexter reads/watch events and documented exceptions remain unchanged. No live data, migrations, deployment, email or documents were changed.
- Next: authoritative shipment-level value storage and selective application, legacy-to-structured mapping, the actual Quote cargo editor/public response, then the remaining original all-mode and live-release gates. Do not infer that the displayed source Quote value is the current Booking total or that all allocation work is complete.

## Authoritative shipment goods value, 5 September 2026

- Added unapplied CLI-generated `20260905151617_booking_shipment_goods_value.sql`: exact numeric shipment amount/currency on the Booking header, separate from every cargo allocation and the accepted source snapshot. Existing Bookings are left explicitly unset rather than backfilled from potentially changed cargo or historical totals. First conversion captures the accepted Quote total; ordinary canonical saves retain omitted values, validate explicit edits/clears and audit actual changes.
- Review adds a separately selectable shipment amount/currency pair. The review note explicitly says cargo allocations remain unchanged. Existing token, latest-acceptance, permission and atomic-apply protections remain; stale observations are rejected and repeated successful applications do not overwrite subsequent operational edits. Legacy cargo-summary application now retains the current cargo declared value instead of implicitly applying the newer shipment total. Older flat Quotes remain separate from the still-unresolved legacy-to-structured mapping case.
- Pre-send readiness rejects invalid values and header/version differences. A real PostgreSQL test executes customer-response preparation and proves that unsaved totals create no response link; matching saved values produce a version-bound pending link. No actual email, PDF storage or provider delivery is involved.
- The existing Booking amount/currency control edits the typed total only when the backend supplies it. It uses exact decimal strings, preserves the original Quote evidence, and leaves individual cargo values/currencies alone. Browser QA rendered the actual editor, draft mutator and review panel with synthetic data and callbacks: amount/currency edits, zero, explicit blank, both English regions, 320px reflow and review-note accessibility were checked; no browser errors were reported. The generic automation select-all shortcut did not select text, so blanking was verified with DOM selection followed by an actual Backspace key. This was not a signed-in save/reload test.
- Focused suite: 24 passed, zero failures/skips, including actual PostgreSQL initial conversion, canonical save, stale review, partial/final application, retry, legacy allocation preservation, immutable history, malformed values, atomic rollback and foreign-actor/function-grant denial. Identity resolution and the broad workspace/document response remain explicit isolated fixture boundaries. Client TypeScript checking passed. No repository-wide green, live RLS or hosted lifecycle claim is made.
- Supabase security guidance informed exact numeric storage, empty search paths, explicit role revocations and negative access checks. No new browser table access, tenant configuration or permission category was introduced. Live schema advisors and migration reconciliation remain release gates; no migration has been applied to a tenant.
- Explicit temporary Dexter exception: the older generic Booking adapter cannot safely distinguish shipment total from first-line declared value. Direct shipment-value read/edit and value-specific Watching are unsupported until a dedicated canonical adapter is connected; the prompt states this rather than pretending generic update_booking provides it. Existing Booking audit events and cargo capabilities remain, with no added recurring LLM evaluation. This exception is incomplete parity, not a completed goal item.
- The final production client build also passed, with the existing large-bundle warnings. The isolated browser and test server were closed. These are existing Multideck-owned controls and panels, not new gallery components or tenant-branding changes. Next work remains the actual Quote cargo editor and public presentation, reviewed legacy mapping, dedicated Dexter parity, full mode depth and controlled live release. The full original goal is still active and below the 95% evidence target.

## Quote goods editor and precision-safe creation, 5 September 2026

- Added the actual multi-line goods editor to Quote Details. Operators select one line rather than editing repeated full forms. Descriptions, package types/quantities, weights, dimensions, HS/origin and basic safety flags persist by stable line ID. Full descriptions use a multiline field; add focuses it, and removal requires confirmation with a recoverable empty state. Submitted cargo renders as readable text rather than disabled inputs. This improves the cargo section only; the rest of the submitted Quote still needs a coherent document-style presentation.
- The real workspace reader and save payload now retain all lines, including an intentional empty list. Totals derive from complete measurements using exact decimal arithmetic; missing measurements stay unknown. Shipment goods value stays separate. Legacy Quotes retain the flat summary until the operator explicitly chooses individual lines. The initial description uses the commodity, not a generic handling label such as Hazardous. Shipment-wide handling/DG details remain alongside basic line flags; detailed per-line dangerous goods and allocation are still unfinished.
- New unapplied migration `20260905160621_quote_open_structured_cargo.sql` seeds one blank goods line through the existing canonical open/save action. No extra post-creation save or reference allocation is introduced. The page waits for creation before allowing edits, and replays of the creation effect share one in-flight request. A focused test executes that actual request expression; it is not a full-page Strict Mode browser test.
- The same migration fixes a precision boundary discovered during integration: new normalised cargo snapshots carry validated decimals as strings so JSON parsing cannot round them before the editor receives them. Typed projection columns remain numeric. Migration application leaves all existing snapshots intact, including submitted versions. Existing snapshots that already expose JSON numbers have not been rewritten or claimed to regain previously lost client precision.
- Tests execute the complete production client load/save functions and customer-safe PDF cargo projection. An extended disposable PostgreSQL test sends that real payload through the actual draft-collapse, normaliser and typed version projection, then reloads it into the client mapper. It proves stable IDs/order, a second line, a 24-place decimal, one mutable draft, explicit clearing and immutable prior evidence. The broad initial save/identity/reference allocator remains an explicit fixture, not proof of the complete tenant creation API. The existing real readiness, conversion and revision test also runs with the new decimal representation.
- Isolated Chrome QA rendered the actual editor/shared controls with synthetic state, not a signed-in Quote or database. Verified line switching retains edits; add/focus; remove cancellation/confirmation; final-line removal and recovery; package dropdown; hazardous flag; long decimal input; read-only absence of editing controls; 320px reflow; both English-region contexts; and reduced-motion recovery. No browser errors were reported. The motion scanner reports zero findings; no custom motion was added. Representative screenshots: `/Users/leewright/.agent-browser/tmp/screenshots/screenshot-1788624959694.png` (wide), `screenshot-1788625184632.png` (narrow read-only), `screenshot-1788625277316.png` (narrow editor).
- This is a Multideck-owned component using shared controls/tokens. Its gallery metadata, source, usage example, preview, Operations navigation entry and Quote links are included. React review informed creation-request reuse and derived summaries without synchronising duplicate state; accessibility guidance informed labels, focus and read-only semantics. Supabase guidance informed the unchanged service-only action and restricted function grants. No tenant branding, provider credentials, reference rules or live data changed.
- **Release remains gated:** the new migration and preceding cargo dependencies are unapplied. Individual Quote cargo Dexter read/write/watch remains explicitly unsupported pending canonical adapters; existing Quote lifecycle events remain event-driven. Legacy-to-structured Booking mapping, richer mode records, complete submitted-Quote summaries, customer response/hosted PDF/email/storage tests and controlled live deployment parity remain required. The full original all-mode goal is active, not 95% complete.
- Final focused result: 13 tests passed, zero failures/skips. Final TypeScript/production build passed, with existing large-bundle warnings. The isolated browser and QA server were closed. Next integration checks must also reconcile per-line safety flags with shipment-wide handling summaries and detailed DG requirements; the separate controls must not imply that a hazardous line is a non-hazardous shipment. No signed-in browser persistence, full reference allocation/permission lifecycle or live release is claimed by this checkpoint.

## Quote cargo safety reconciliation, 5 September 2026

- Cargo-line hazardous and temperature-controlled flags now appear in the Quote shipment summary without becoming copied manual choices. Inherited controls explain where to change the flags; keyboard activation cannot silently remove them. Manual Fragile and other handling selections remain independent when a line flag is removed. Flagged lines are identified in the line selector. An open hazardous-details editor closes when the Quote becomes read-only and cannot save through its callback while disabled.
- New unapplied migration `20260905163327_quote_cargo_safety_summary.sql` enforces the same summary in the canonical save path, including for older callers with a stale General merchandise label. It keeps the original manual handling separately, delegates existing save/version/permission/audit behaviour, and leaves existing history untouched. Private helpers and the renamed save implementation have no application-role execution grant; the canonical save remains service-only.
- Customer document handling comes only from recognised labels and typed flags in the selected version. Arbitrary internal handling notes are excluded. Browser inspection caught that adding a fifth dataset field alone did not render it: the existing template addressed four fixed positions. The template now includes the handling row, with a regression that failed before the template correction and passes afterwards, including manual-only handling.
- Local evidence: the focused client/document/PostgreSQL suite passed 14 tests before the final template fix; the six document tests passed after the fix. The full Quote Edge Function import graph passed Deno checking. The existing current-body client TypeScript and production build passed, with large-bundle warnings. Isolated Chrome checks used synthetic state, no tenant connection: inherited keyboard behaviour, independent manual flags, read-only during an open editor, 320px reflow and the alternate English-region context. No browser errors were reported. The final HTML document was visually inspected with the handling row visible; this is not a new hosted Carbone/PDF/email/storage proof.
- Existing Multideck-owned controls and gallery previews were reused. Carbone guidance informed the existing-template mapping and escaped rendering path. No new branding scope, authority, notification type or recurring AI evaluation was introduced. The explicit temporary unsupported individual-Quote-cargo Dexter read/edit/watch exception remains; it is incomplete parity, not a completed goal item.
- Remaining integration clash: Booking still edits its old Known cargo text independently of typed hazard/temperature fields. Its generic cargo mutator has no boolean conversion and must not receive the string `false` as a truthy flag. The next operational work must reconcile this boundary and preserve existing dangerous-goods evidence. Detailed DG records, legacy-to-structured mapping, all-mode operational depth, full submitted summaries, Dexter parity, hosted lifecycle verification and controlled live deployment remain required. No release or 95% completion is claimed.
- Final combined result after the template correction: 15 tests passed, zero failed/skipped; Deno checking and `git diff --check` passed. The isolated browser was closed and its identified local QA server stopped. No customer data or provider messages were changed.

## Booking cargo safety controls, 5 September 2026

- Replaced the mutually exclusive Known cargo selector with independent Hazardous and Temperature controlled controls on the selected cargo line, plus Other handling for general merchandise/oversized/fragile/food-grade descriptions. Existing custom descriptions remain available as the current value. Missing typed flags render Not recorded, including read-only views; they are not inferred as No from a missing value. Earlier positive handling text that disagrees with a flag remains visible with a review explanation. This is not a dangerous-goods classification/compliance decision.
- The real Booking draft updater now converts Yes/No to JSON booleans, keeps the compatibility handling text consistent with explicit flags, and preserves unconfirmed legacy safety text when only other handling changes. Invalid flag values, out-of-range/stale indices and no-cargo views cannot create unintended rows. Other lines, stable IDs, Quote evidence and existing detailed cargo data remain untouched. Ordinary numeric cargo editing still has the previously identified JavaScript-number precision limitation; it was not silently claimed fixed here.
- Shared production-code test fixtures execute the actual updater. The resulting false/true payload was passed to the actual Booking save function in disposable PostgreSQL, verifying stored boolean values and retained linked DG evidence. Existing stable-item identity, permission-denial, audit, Dexter approval, read and event-driven watch tests remain green. Auth/permission resolvers and broad workspace reads in that fixture remain explicit limitations, not live tenant proof.
- Final focused result: 16 tests passed, zero failures/skips. TypeScript and the final production client build passed, with the existing large-bundle warnings. Isolated Chrome rendered the actual shared fields, their production expressions and updater with synthetic state: keyboard selection, focus restoration, independent lines/flags, 320px reflow, both English-region contexts, read-only removal of inputs, and missing legacy flags were checked. No browser errors were reported. A stale browser locator sequence was detected and corrected with fresh snapshots/semantic locators; read-only and region assertions were then rerun successfully. This is not a signed-in save/reload browser test.
- Existing Multideck-owned controls/tokens were reused; no new gallery component, route or tenant-branding scope was introduced. Accessibility guidance informed explicit names/states, missing-value text and keyboard verification. The already-allowlisted Booking cargo safety fields and existing deterministic watch events remain the Dexter boundary; no new authority or recurring model evaluation was added. Compatibility-text changes made through older/non-UI paths may still require review; typed flags remain the operational evidence.
- Detailed DG editing and mode-specific readiness, cargo precision, legacy-to-structured handover, full submitted Quote summaries, the outstanding Dexter exceptions, all-mode operational depth and controlled live release remain required. No database migration or external deployment was performed in this checkpoint. The full original goal remains active and below its 95% evidence target.

## Booking cargo decimal boundary, 5 September 2026

- The actual Booking draft updater now retains numeric input as text, including incomplete/invalid input and explicit clears. It no longer rounds long decimals through JavaScript numbers, removes arbitrary commas, or turns invalid input silently into null. Existing booleans, cargo IDs, other lines, nested DG evidence and accepted Quote snapshots remain independent.
- New unapplied migration `20260905172223_booking_cargo_decimal_boundary.sql` validates all nine existing typed cargo numeric fields before the canonical save, full workspace save and the older direct dimensions stage. Valid grouped thousands are normalised; unsupported precision/range, malformed separators, negative/non-finite and non-numeric values receive a line-specific English error. Existing PostgreSQL column scales remain authoritative (two decimal places for pieces/weights/dimensions, six for package quantity/volume, four for cargo line value). No historical values are rewritten and no column is widened. Blank/null is an explicit clear; zero stays zero.
- The final Booking cargo read overlays exact decimal text from the typed row after older dimension projections run. It checks current Booking read permission, active actor/company/office scope, active cargo ownership and returned cardinality. Empty cargo remains an empty list. Internal renamed functions/helpers are not executable by browser or service roles; the existing canonical service endpoints retain their grants. Existing permission checks and transactional audits remain in the delegated implementations.
- Dexter's allowlisted non-financial cargo read and before/after/watch projection now preserve decimal text too. Its descriptions explain exact values and prohibit rounding proposals to pass validation. Existing approved edits use the same canonical precision gate; watch comparisons remain deterministic numeric comparisons. No prices/raw cargo JSON were added to that read/watch domain, no new write authority was introduced, and no recurring AI evaluation was added.
- Local evidence: 14 focused tests passed, zero failures/skips. The disposable PostgreSQL fixture executes the real client updater's serialised payload through the actual stable save, dimensions and new wrappers, proving maximum supported decimals, save/reload identity, clears, later-line atomic rejection, direct-stage rejection, foreign-office/unauthorised denial and restricted grants. Existing Dexter approval, retry, pause/resume, owner RLS and revoked-access assertions still pass; an added decimal threshold case fires once and a no-op stays silent. The existing broad workspace and Auth/permission resolvers remain explicit fixtures, not a deployed tenant proof.
- TypeScript and the production build passed with existing large-bundle warnings. Isolated Chrome used the actual numeric field expressions, shared input and updater: exact long input, invalid input retained after blur, keyboard focus, other-line preservation, 320px reflow, alternate English region and read-only text were checked. No page errors were reported. The isolated browser and identified QA server were closed. This is not a signed-in browser save/reload against a real tenant; database persistence was separately exercised above.
- Release remains gated: this migration and earlier cargo migrations have not been applied live, and these changes have not been pushed/deployed. Container numeric fields and the separate Booking freight-charge amount are outside this cargo-only precision correction and still need their own review. Detailed DG/mode records, legacy-to-structured handover, complete submitted Quote summaries, outstanding Dexter parity, hosted response/PDF/email/storage lifecycles and controlled deployment remain part of the original full goal. This checkpoint does not establish 95% completion.

## Submitted Quote records, 5 September 2026

- Replaced disabled-form presentation for submitted Quote Overview, Details and Quote charges with an internal, snapshot-only record view. Original references stay unsuffixed; subsequent versions show the master reference with ` - V2` and onward. The existing version menu and New version flow remain; mutable drafts retain their existing editors and state.
- Fixed current and historical version projection: missing content no longer inherits from the current Quote header or current organisation/terms lookups. Both the latest submitted version and older selected versions use their own saved snapshot. Invalid or unavailable snapshots block the content tabs visibly; copy and blank revision attempts cannot silently use the current draft as an alternative source.
- Found the shared charge editor resolving even read-only rows against current finance rates. Submitted charge views now render their recorded amounts, currencies, rates, base amounts, supplier/source and notes directly, without mounting that editor or the current-rate context panel. The draft charge editor is unchanged. This remains an authenticated internal view; costs and internal notes are not added to the customer response or customer PDF.
- The view preserves all saved cargo rows, routing legs and container requests, full multiline text, exact decimal strings, zero values and missing/false safety distinctions. Explicit empty structured lists do not revive legacy cargo/container summaries. Missing information is labelled Not recorded; unreadable nested lists produce an alert. Internal supporting information uses native details/summary disclosure. No arbitrary snapshot JSON is rendered.
- Final focused evidence: 16 tests passed with no failures/skips. Tests execute the real page's selection expressions, submitted-panel gate and unavailable-source revision guard, plus the actual rendered record and existing Quote cargo mapping/PDF projection. They cover current and historical version isolation, working-draft preservation, missing snapshots, saved rates, original/V2 labels, internal versus overview detail and escaped user text. The first four render-test failures were an isolated esbuild JSX runtime mismatch, corrected in the fixture; production TypeScript/build passed.
- Isolated Chrome QA rendered the actual record and shared primitives with synthetic data: version switching, narrow 320px reflow, full long values, both English date formats, zero edit controls, saved charge amounts/rates, missing-snapshot recovery, keyboard Enter/Space with visible focus and reduced motion passed. 200% CSS zoom was checked without horizontal overflow (not an OS/browser accessibility zoom certification). No page errors were reported. This does not prove a signed-in tenant browser/API/database send/revise lifecycle or PDF storage.
- This is a Multideck-owned internal record assembly, not a new reusable gallery component or customer-branded output. It reuses Surface and updates that component's Quote quick link. Layout, typography and accessibility skills informed hierarchy, full-value wrapping and native disclosure; React review confirmed derived state without new fetches/effects. No backend capability, permission, Dexter authority, live data or deployment was changed.
- Original goal still includes detailed Sea/Air/Road/Rail operational records, legacy-to-structured handover, remaining financial boundaries and Dexter exceptions, hosted customer response/email/PDF/storage verification, migration parity and controlled deployment. This local checkpoint does not establish 95% completion or make the release gate green.

## Per-leg operational references and single routing save, 5 September 2026

- Added compact per-leg Operational details using existing typed routing columns: Sea master/house bills and voyage; Air MAWB/HAWB; Road CMR/forwarder/trailer; Rail CIM/SMGS/forwarder; and service level. Selection follows each actual leg, including mixed-mode jobs, not the overall job mode. Unsupported/non-transport modes do not gain invented transport controls. Shared inputs enforce existing text lengths, have linked labels, and retain full read-only values. Add/remove controls respect read-only state; adding stops at the existing 30-leg boundary. Invalid route indices cannot create phantom draft legs.
- Found a second routing clash while tracing mode changes: the full workspace save first wrote a legacy first-route summary using the overall job mode, then saved the real legs again. New unapplied `20260905182616_booking_route_single_save_authority.sql` removes the legacy `route` payload only from that first stage when `routes` is explicitly present. The existing leg stage alone writes routes. Legacy-only callers still work; an explicit empty routing list does not revive stale legacy data or remove existing operational legs. Public direction/decimal wrappers, permissions, audit, cargo, party and detail stages remain unchanged. The migration checks the expected implementation anchors and retains private function grants.
- Local PostgreSQL regression **reproduces the duplicate first leg before the new migration and passes afterwards**. It executes the actual canonical primary save, actual route save and actual orchestration stage. Tests verify mixed-mode creation, repeated stable IDs, reordering without cross-leg copying, no transient job-mode overwrite, explicit clears, legacy-only saves, empty-list handling, invalid-later-leg rollback including audit, actor/company/foreign-route denial and restricted grants. Identity/permission resolution, broad reads and unrelated cargo/detail stages remain explicit fixtures; this is not a full signed-in tenant lifecycle or live isolation certification.
- Focused final result: 14 passed, zero failed/skipped. Client TypeScript passed after the final field changes; the preceding production build passed with existing bundle-size warnings. Isolated Chrome used the actual production field/disclosure expressions and draft updater with synthetic state. Desktop and 320px layouts, Sea/Air/Road/Rail field selection, per-leg editing, label association, long read-only reference wrapping, English-region contexts, reduced motion and native Enter/Space disclosure with visible focus were checked; no page errors were reported. The final Rail read-only screenshot is `/Users/leewright/.agent-browser/tmp/screenshots/screenshot-1788633022856.png`. This is not browser-to-live-database proof.
- These are Multideck-owned controls composed from existing primitives, not a new reusable gallery component. Per-leg Dexter read/edit/watch adapters remain **explicitly unsupported**; the prompt directs requests to the exact route step rather than claiming generic first-leg support. Existing approved Booking/cargo actions and deterministic watches are unchanged. This exception remains unfinished parity.
- **Release blocker and immediate next work:** the generic master/house reference columns still lack mode provenance. Browser testing proved that manually changing a Sea leg to Air can display its previous HBL under the HAWB label. The single-save correction prevents unintended intermediate mode changes but does not solve deliberate mode changes. Add explicit mode review and audit-safe reference handling across manual Booking edits and accepted Quote application; do not silently clear history or present retained values as valid for the new mode. Preserve all existing submitted Quote and document evidence. Dedicated mode records, broader operational fields, legacy cargo handover, Dexter parity and controlled hosted lifecycle/deployment verification remain in the original goal. No new migration was applied live and no deployment or 95% completion is claimed.

## Routing mode review and reference history, 5 September 2026

- Manual per-leg mode selection now opens the existing confirmation dialog before changing draft state. Cancel/Escape leaves the mode and values unchanged. Confirmation starts shared master/house/carrier-booking references and generic transport service blank; mode-specific typed fields (vessel/voyage, flight, vehicle/trailer, rail service) remain separate and retained for review. The dialog explains the effect and asks the operator to review carrier/schedule/details. It does not change the Quote or documents. Replaced/reordered draft legs cannot receive a stale confirmation; the user gets a retry explanation.
- The draft's mode-review evidence is bound to the **persisted** route's mode and shared references, not to intermediate unsaved mode choices. The new unapplied `20260905183528_booking_route_mode_reference_history.sql` adds a private before-update trigger on routing. An outdated review rejects the transaction. A reviewed operator may deliberately re-enter even an identical reference. An older/accepted-Quote write without that review cannot carry unchanged shared references into a different mode; genuinely new reference values remain. Compatibility JSON is kept consistent, and the one-use review is removed from the saved row.
- Mode/reference changes record before/after shared references, route ID, mode, actor and previous mode-specific transport evidence in the existing Booking audit events, in the same transaction. A failure rolls the event back with the route update; unrelated/no-op updates create no reference audit noise. No new table grants, public RPC, permissions or background model calls were introduced. Audit UI now exposes an escaped, full-value before/after reference comparison using each side's own mode labels.
- Final combined local result: 16 tests passed, zero failures/skips. The actual client updater/helper, actual rendered audit assembly and disposable PostgreSQL save/trigger/orchestration are exercised. Regressions fail before the relevant migrations and pass afterwards. Checks cover repeated unsaved mode choices, aliases in the client helper, original state preservation, reviewed identical-value re-entry, automatic history protection for older SQL update paths, stale review rejection/rollback, compatibility clearing, audit attribution and restricted trigger execution. Existing fixture limitations around Auth/permission resolution, broad workspace reads and unrelated save stages remain; a direct SQL update proves trigger coverage, **not** the complete hosted accepted-Quote application lifecycle.
- Production client build and final TypeScript passed with existing bundle warnings. The full Dexter Edge Function import graph passed Deno checking. One compiler invocation from the repository root selected an unrelated `tsc` package and failed; it made no project changes, and verification was rerun successfully from `multideck.client` with the project's compiler.
- Isolated Chrome rendered the production field, review callbacks/dialog and updater with synthetic records and shared controls. It proved Cancel/Escape preservation and focus restoration, confirmation clearing only the selected leg's shared references, retention of its vessel and other legs, 320px reflow, both English-region contexts, reduced motion, and keyboard Tab/Enter confirmation. An initial keyboard sequence reselected the existing option rather than opening review; fresh snapshots and explicit selection corrected the check. The fixture's mode vocabulary was then aligned with the real uppercase mode policy and cancel/confirm rerun successfully. No page errors were reported. Screenshot `/Users/leewright/.agent-browser/tmp/screenshots/screenshot-1788633856254.png` shows the narrow review layout; actual audit rendering is tested separately, not a live browser API response.
- Better UI/accessibility guidance informed reuse of the existing dialog, least-destructive initial focus, keyboard return and readable audit values; Supabase guidance informed private execution grants and transactional audit. These remain Multideck-owned internal surfaces with no new reusable gallery component. Dexter explicitly directs per-leg history/read/edit/watch requests to the operator screens until scoped adapters exist; this is a documented temporary parity gap, not complete AI support.
- **Remaining gates:** reconcile overall Job mode versus per-leg mode, and verify warnings/field selection for nested routing changes from accepted Quotes through the full public review/apply lifecycle. Hosted responses, email/PDF/storage, outstanding Dexter adapters, detailed DG/equipment/milestones, deeper Sea/Air/Road/Rail workflows, legacy cargo handover and controlled migration/deployment parity remain required. The previous silent per-leg HBL-to-HAWB draft transition now has local protection, but neither live deployment nor the original 95% goal is claimed.

## Accepted Quote routing mode review, 5 September 2026

- New unapplied `20260905185420_quote_routing_mode_review_guard.sql` extends the existing difference chain instead of replacing cargo/payer/value behaviour. A routing-plan change now carries the Mode warning when it changes an overlapping Quote-owned leg's mode or introduces/removes a distinct mode. Dictionary aliases use the real normaliser; another same-mode leg or a date-only change does not alone require a Mode popup. Unknown modes are not inferred as Road and cannot be applied.
- The v2 public apply boundary recomputes the routing condition from its refreshed review, retaining actor/company permission, latest accepted version, exact review token, selection and idempotence checks. The internal confirmed routing stage also checks it. The old unconfirmed/confirmed stages are no longer direct service-role RPC entrypoints: the current Edge Function already calls v2, and these stages remain callable internally by its definer. **Coordinated release prerequisite:** verify the target is running that v2 Edge path before revoking older RPC entrypoints; do not apply this migration alone to an older deployed workflow.
- Fixed a related actual apply defect: reducing a multi-leg accepted Quote to A-B previously returned without writing routes but still recorded routing as applied. It now applies the one-leg plan, supersedes removed Quote-owned rows and keeps Booking-only content intact. The existing reference-history trigger no longer rewrites compatibility JSON on an unrelated route reorder. Changed-mode references retain their previous mode-labelled values in audit. Successful explicit Quote mode approval also records its own actor/version/review/token receipt; failed transactions and retries do not create extra receipts.
- The existing Multideck-owned review panel now exposes a native Inspect routing plan disclosure with all three sources, ordered legs, modes, origin/destination names and codes, planned dates, carrier and service. Previously it only displayed the line count. Inspecting is outside the selection label. Selected routing warnings and Apply all open the existing confirmation dialog; unrelated selections and date-only changes do not. Confirmation is bound to the review token that opened it, so a newly received comparison requires another review. Shared-reference clearing is explained explicitly.
- Local combined evidence: 17 tests passed, zero failures/skips, including the real PostgreSQL review/apply/routing chain plus current reference trigger. New scenarios cover nested Sea-to-Air with unchanged overall Sea mode, reduced/expanded plans, same-mode schedule changes, an actual operational date edit invalidating the token, foreign actors, an older accepted version, invalid later-leg rollback, unsupported modes, source snapshot/original Booking evidence preservation, unchanged Booking-only content, reference and approval audit receipts, retries and execution grants. The fixture now uses the actual dictionary-backed mode normaliser for these cases. Identity resolution, initial review creation, broad document workspace and external HTTP/provider boundaries remain declared fixtures, not hosted lifecycle proof.
- Production build/TypeScript and the full Dexter Edge import graph passed; existing bundle-size warnings remain. Isolated Chrome exercised actual review components and callbacks with synthetic data: Inspect using Enter/Space without selecting Apply, full mode/date/carrier text, Apply all mode warning, least-destructive initial focus, Escape preservation and focus return, keyboard approval with `confirmed:true`, and date-only application with `confirmed:false`. At 320px with reduced motion and en-US dates there was no horizontal overflow; en-GB was also checked. No browser errors were reported. Narrow disclosure screenshot: `/Users/leewright/.agent-browser/tmp/screenshots/screenshot-1788635649256.png`. This is not a signed-in tenant browser-to-database run.
- Supabase/Postgres guidance informed restricted entrypoints and transactional approval/audit; UI/accessibility guidance informed reuse, readable disclosure and focus preservation. No new reusable gallery component, tenant branding, auth configuration or autonomous AI action was introduced. Dexter explicitly routes nested routing revisions to the operator review; dedicated route read/edit/watch adapters remain an unfinished, documented parity exception.
- **Next concrete clash:** Quote `removeLastRoutingLeg` can retain one explicit leg with a different mode, while the current single-leg screen/projection falls back to the overall Quote mode. Resolve single-leg versus header authority and audit-safe overall Job mode handling before declaring mode consistency complete. Deeper mode operations, equipment/DG/milestones, legacy cargo mapping, remaining Dexter adapters, hosted email/PDF/storage/response flows, migration reconciliation and controlled deployment still remain in the original goal. No live migration or deployment was performed; the 95% target remains unproven.

## Explicit single-leg routing authority, 5 September 2026

- Fixed the actual Quote editor loss: `quoteRoutingLegsValue` previously discarded any plan with only one leg. An explicit final leg now stays serialised, visible and editable with its identity, own mode, carrier, service and dates. The default Quote with no explicit plan remains compact A-B. Removing the last remaining leg is disabled; read-only callbacks and invalid indices cannot mutate the plan. Re-expansion starts from the preserved leg, not from header defaults.
- Add/edit/remove now share the same endpoint/date summary update. Adding an unfinished last leg clears the final summary rather than presenting the previous destination and ETA as if still current. Reducing the plan keeps deliberate empty values instead of reviving removed-leg dates/places. Header Mode remains independent; this change does not silently classify the shipment or introduce Quote drift warnings for Booking-only edits.
- New unapplied `20260905192002_quote_single_leg_routing_authority.sql` changes the existing private routing projection/comparison implementation, retaining its later cargo/payer/value/approval wrappers and restricted grants. Any non-empty explicit routing array is authoritative. Its internal marker makes a single explicit plan selectable through Routing plan, without duplicate flat route fields; legacy A-B retains individual field selection, including old baselines without the marker. Empty UN/LOCODE values no longer suppress a deliberately entered place name. No historical snapshot rows are rewritten.
- Disposable PostgreSQL reproduces the old single-leg Air-to-header-Sea loss before applying the migration, then verifies actual projection, initial conversion plus routing application, single-leg date revisions, mode-only approval, return to legacy A-B, stable Booking route identity, original submitted evidence and private grants. The existing mixed-mode, stale/foreign approval, reduction, rollback and retry fixture is rerun after the new migration. Identity/numbering, review creation and broad reads remain explicit fixtures: this is not proof of the entire hosted acceptance pipeline.
- Focused client/PG/submitted-document suite: 19 passed, zero failures/skips. Existing submitted summary and actual customer PDF dataset tests already cover one explicit route and continue to pass. Client production build passed with existing bundle-size warnings; final TypeScript and Dexter Edge import checks passed. A screenshot exposed an existing per-leg date field width overlapping its neighbour; those two controls now fit their grid cells, verified visually after the correction.
- Isolated Chrome used the real routing JSX, callbacks and shared controls with synthetic state. Verified reduction to one Air leg, retained carrier edits through serialise/reopen, keyboard removal and service selection, read-only controls, 320px no-overflow and both English date variants. No browser errors were reported. Narrow screenshot: `/Users/leewright/.agent-browser/tmp/screenshots/screenshot-1788636935253.png`; corrected wide read-only screenshot: `/Users/leewright/.agent-browser/tmp/screenshots/screenshot-1788636948866.png`. No signed-in tenant save or hosted PDF/email/storage was performed. The test browser and server were closed.
- Supabase/Postgres guidance informed the private migration boundary; UI/accessibility guidance informed reuse and keyboard verification. These are existing Multideck-owned controls, not a new gallery component. Dexter explicitly warns against inferring a single route from header values; dedicated route read/edit/watch remains an unfinished, documented exception.
- **Next:** overall Quote/Booking mode review and consistent policy when the header mode differs from actual legs remain open. Continue through deeper mode-specific equipment/DG/milestones, legacy cargo handover, Dexter parity, full hosted lifecycle verification and controlled release. No live migration, push or deployment is claimed here; the full goal remains active.
- Additional actual Quote load/save mapper and sync-boundary regression checks passed 15 tests: 34 focused tests in total, zero failures/skips. These supplement, not replace, the declared hosted lifecycle limitations above.

## Overall mode and physical leg separation, 5 September 2026

- The previous user check-in was observational, not implementation progress. Resumed from the existing uncommitted mode UI work and reproduced two actual database defects before fixing them: applying overall Mode rewrote an explicit leg; a later date-only compatibility save also relabelled an independently edited leg using the header mode.
- Quote and Booking Details now confirm overall mode changes before altering draft state. Cancel/Escape keeps the original values; incompatible shipment type clears only on confirmation. Existing routes, their references, cargo and equipment remain untouched for review. Confirmation is bound to record identity, original mode/service and routing state; stale confirmations reject, and read-only/permission changes cannot apply them. The active production Quote routes both use `QuoteDetailsPanelV2`; the older non-CargoWise setup variant is not mounted by App's current routes.
- The shared field policy now considers actual routing-leg modes as well as the commercial mode, not only on jobs labelled Multimodal. Sea plus a Road leg exposes the relevant Road operational fields, without turning the job into Multimodal. A standard overall mode used by no actual leg produces a soft local review message. This is not a Quote out-of-sync flag, does not change stored status, and does not rewrite a route. Header-implied fields remain visible while deeper mixed-mode readiness and equipment rules are still being completed.
- New **unapplied** migration `20260905195412_quote_overall_mode_route_authority.sql` preserves the existing checked v2 approval/audit chain. An overall Mode selection no longer enters the compatibility route payload for explicit Quote plans, separately selected routing, multiple Booking legs or a sole leg whose mode has diverged. The original derived A-B mode behavior remains when there is exactly one matching legacy leg. The private legacy saver now honors an explicit `route.mode` and otherwise preserves an existing leg's mode during date/address/reference edits. Initial legacy route creation may derive its mode from the job. Existing rows and submitted snapshots are not rewritten; internal functions remain revoked from external roles.
- Disposable PostgreSQL exercises eight real review/apply cases: legacy A-B, explicit one/two legs, Booking-added legs, a manually changed sole leg, explicit-to-legacy partial application (including an old baseline without the explicit marker), combined Mode + Routing plan, and Mode + collection address. It checks rejection without approval, unchanged unselected legs/references, original snapshot and Booking evidence, idempotent retry, audit preservation, separate explicit route changes, no intermediate combined-apply mode, and restricted internal grants. Existing mixed-mode/stale/foreign/rollback tests remain in the same suite. Parent fixtures still replace identity, numbering and broad workspace reads: this is not the full hosted acceptance lifecycle.
- Final focused result: **39 tests passed, zero failures/skips**. Client production build and TypeScript passed (existing large-chunk warnings); the full Dexter import graph passed Deno checking with global-cache dependency resolution. The first default Deno invocation could not resolve the repository's npm package layout; no dependency/config change was made. An outdated source contract for inherited cargo flags was aligned with the already-existing production behavior; no cargo behavior was weakened.
- Isolated Chrome exercised real production fields, handlers, dialogs and shared controls against synthetic records: both English-region contexts, Cancel/Escape focus restoration, keyboard confirmation, read-only state, preserved routes/references, 320px and desktop, reduced motion, and no reported page errors. A horizontal overflow originated in the fixture's raw JSON output/grid and was corrected in the fixture; final narrow Quote and Booking surfaces measured 320px scroll width at 320px viewport. Viewed screenshots: `screenshot-1788638619173.png` (Quote narrow), `screenshot-1788638687833.png` (Booking narrow confirmation), `screenshot-1788638848148.png` (Booking desktop) under `/Users/leewright/.agent-browser/tmp/screenshots/`. No live tenant record was modified by these checks.
- Supabase/Postgres guidance informed bounded migration locks and private transactional changes; UI/accessibility and React guidance informed shared controls, derived policy, stale-review guards and least-destructive focus. These remain Multideck-owned surfaces, with no new gallery component or tenant branding change. Dexter documents the overall-mode versus leg distinction and directs dedicated mode-review/watch requests to Details until its scoped adapters exist; that parity gap is still unfinished, not an autonomous capability.
- **Remaining original goal:** deeper Sea/Air/Road/Rail equipment, DG and milestone workflows; mixed-mode readiness; legacy cargo handover; remaining Dexter adapters; hosted customer response/email/PDF/storage lifecycle verification; migration reconciliation and controlled deployment without team setup changes. This checkpoint is local only: no push, live migration or deployment. The full 95% target remains unproven and the goal stays active.
