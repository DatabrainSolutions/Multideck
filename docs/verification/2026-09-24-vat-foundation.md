# UK VAT foundation verification — 24 September 2026

Local only. `gb-vat-mtd` remains `foundation`; no tenant migration or HMRC
connection was deployed.

## Verified

- `node --test supabase/tests/uk-vat-period-foundation-postgres.test.mjs supabase/tests/uk-vat-nine-box.test.mjs` passed all six cases against a disposable PostgreSQL fixture and the actual migration SQL.
- The fixture checks separate legal-entity registration, non-overlapping periods, service-only tables with RLS, posted source matching, immutable evidence and decisions, period-specific calculation lines, UK posting capture and non-UK isolation.
- The fixture exercises idempotent draft-period creation, approved-tax-code review, signed credit-note box calculation, missing-review denial, registration drift and immediate actor revocation.
- The pure nine-box test checks that source lines retain four decimal places while box totals round after aggregation, including small credits.
- The PostgreSQL fixture checks native source-to-ledger amount matching for a sale, credit and purchase; a changed VAT posting produces a mismatch and a different calculation digest. Matching source lines still leave approval unavailable pending whole-period controls.
- The fixture checks explicit transaction VAT sign-off with a server-recorded date, actor, reason and source digest. A draft does not date transactions automatically; a read-only colleague and a foreign-company actor cannot sign off; repeating the same sign-off preserves its original date; a changed posting blocks another sign-off. The calculation drill-down and finance-document rollup show the date, while a foreign-company actor cannot read it.
- Historical posted-line coverage detects missing captures. Bounded backfill records the operator and reason, writes an audit event, remains idempotent, and leaves VAT treatment unreviewed. A separate-company actor with the same nominal permission is denied both coverage and backfill.
- An unresolved review queue shows the source document and amounts without inferring its VAT tax point. A same-company Compliance viewer can read a colleague's item but cannot review it; a foreign-company actor cannot read the queue. Review derives supplier treatment from the approved tax code, records an immutable decision and audit event, removes the item from the queue, and feeds boxes 4 and 7 in a new draft calculation.
- The review queue now pages by recorded timestamp and evidence ID, retaining the full unresolved count. The PostgreSQL fixture verifies two same-timestamp pages neither repeat nor skip an evidence item and rejects an incomplete cursor; the operator workspace can load further pages.
- The period list counts distinct transactions and matching dated sign-offs for its latest calculation revision; the fixture checks zero before sign-off and one afterward. This remains a snapshot review count, not a whole-period VAT control or filing approval.
- The finance Edge route exposes period listing/preparation, draft calculation, coverage, review queue, review and backfill behind Compliance permissions and a same-company legal-entity check. `esbuild` parsed the changed Edge function successfully; the route has not been exercised against a running tenant.
- The finance Edge route also exposes bounded, entity-scoped calculation detail with box-line evidence and reviewed decision IDs. The PostgreSQL fixture checks same-company viewer access, foreign-company denial and page bounds. This has not been exercised in a running tenant.
- The client finance API has typed calls for these draft operations and transaction sign-off. The finance document view displays the date once all its current posted lines have been signed off. A local UK VAT review workspace now presents coverage, evidence review, periods, nine boxes and transaction sign-off to permissioned users. No live browser journey has been verified yet.
- `multideck.client/node_modules/.bin/tsc -b multideck.client` passed after adding the typed calls.
- `node --test supabase/tests/uk-vat-hmrc-connection-postgres.test.mjs supabase/tests/uk-vat-hmrc-oauth.test.mjs` passed five cases. The database fixture checks one-use consent state, tenant and company boundaries, registration and actor revocation, a production gate, Vault-only token storage, token-free status, serialised refresh leases, token rotation, uncertain-outcome reauthorisation and audit. The protocol tests check HMRC-hosted redirect, PKCE, server-side code exchange and renewable VAT scope validation.
- The HMRC protocol tests now also check sandbox/production obligations URLs, bounded date searches, opaque period-key validation, duplicate/malformed response rejection and exact matching to one open obligation. A fail-closed builder checks presence and basic shape of all 16 web-app-via-server fraud headers. `node --test supabase/tests/uk-vat-hmrc-oauth.test.mjs` passed seven cases. These are local contracts, not an HMRC sandbox call, a verified obligation or evidence of HMRC header acceptance.
- The fraud-header builder now rejects malformed structured screen, window, MFA, forwarded-hop, license and version values, private client IPs and HTTP/HTTPS server ports as a purported client source port. The focused protocol suite passed seven cases and `git diff --check` passed. These shape checks do not establish that values came from a trusted network or MFA source, and the required TCP source-port and MFA capture remain unimplemented. No VAT API request was enabled.
- A backend-only obligations GET contract now checks the environment-specific URL, versioned `Accept`, bearer header, fraud-header shape, redirect rejection, parsed obligation and token-free errors. The focused protocol suite passed eight cases. No Edge route calls the helper, and no HMRC sandbox request or header conformance test has run.
- Against HMRC's published VAT API 1.0 OpenAPI contract, the obligation parser now requires a four-character period key, including a `#` case. A new response parser validates the 201 submission receipt references and optional payment fields. The focused protocol suite passed nine cases and the HMRC Edge bundle parsed. No submission call, dispatch record or approval lock exists; this local parser does not establish receipt from HMRC.
- The nine-box kernel and PostgreSQL draft now derive Box 3 from the rounded Boxes 1 and 2, then Box 5 from rounded Boxes 3 and 4, matching HMRC's submitted-value checks. A fractional sale/purchase fixture exposes the prior one-penny mismatch and verifies the corrected Box 5 while preserving four-decimal source lines. The six pure nine-box cases and focused PostgreSQL fixture passed. Filing still requires a separate approved payload projection and sandbox checks.
- The stored calculation is now identified as `uk-standard-v2`, and its digest includes that version and the nine rounded boxes. This prevents a future algorithm change from reusing a source digest and making an older dated sign-off or control review appear current. The focused PostgreSQL fixture passed after this change; the full regression above ran before this final fingerprint edit.
- Standard-scheme review now recognises approved zero-rated and exempt supplier purchases only when both the tax rate and source/reporting VAT are zero. These purchase lines contribute net value to Box 7, never Box 4. The native-posting fixture verifies both treatments, one net ledger line and no tax line, a matched source bridge, and rejection of VAT incorrectly charged on a zero-rate purchase. The seven pure nine-box cases and focused PostgreSQL fixture passed. The calculation version is now `uk-standard-v3` and its fingerprint includes the changed result.
- After the zero-tax purchase change, the full serial access regression passed: 101 PostgreSQL database/access cases and 10 boundary contracts, zero failures. `git diff --check` passed. This remains local; no tenant migration or live finance document journey has been performed.
- Draft recalculation now rechecks the reviewed tax code's effective-date snapshot and its coverage of the decision's tax point. The PostgreSQL fixture changes the effective end date after review, verifies rejection, then restores it. The focused fixture passed. The full serial access regression passed afterward: 101 database/access cases and 10 boundary contracts, zero failures; `git diff --check` passed.
- `esbuild` parsed the new `hmrc-vat-oauth` Edge Function. This checks syntax, not a live callback or HMRC sandbox exchange.
- The typed HMRC client helper sends the browser only to HMRC's exact authorisation host; `multideck.client/node_modules/.bin/tsc -b multideck.client` passed. The operator button was added later, below.
- `git diff --check` passed.
- `PG_TEST_CONCURRENCY=1 node supabase/tests/run-data-access-regression.mjs` passed outside the sandbox after native source-to-ledger checks were added, with the HMRC connection case included: 100 database/access cases and 10 focused boundary contracts, zero failures.
- The six focused VAT cases passed after the review queue, period listing and audit changes.
- After adding transaction sign-off and document date projection, the PostgreSQL VAT fixture, client TypeScript check, finance Edge function parse, and full serial access regression passed locally again (100 database/access cases and 10 boundary contracts).
- After adding the operator UK VAT workspace and company-scoped UK entity list, client TypeScript, the finance Edge function parse, `git diff --check`, and the serial access regression passed again (100 database/access cases and 10 boundary contracts). The workspace has not been exercised against a deployed tenant, so browser happy/failure paths and persistence remain unverified.
- `npm run build` completed for the client with the `/finance/vat` route. The build reported existing bundle-size and mixed static/dynamic import warnings; it is not evidence of a live tenant workflow.
- After cursor paging and period sign-off counts, the focused PostgreSQL VAT fixture, client TypeScript check, finance Edge parse, `git diff --check`, and serial access regression passed again (100 database/access cases and 10 boundary contracts).
- A source-by-source VAT account view now groups each immutable calculation revision by transaction, preserves its box contributions and dated sign-off, and exposes both raw source totals and the rounded return boxes. The PostgreSQL fixture verifies totals, a single row per source, a dated sign-off, bounded pages, same-company viewing and cross-company denial. Client TypeScript, Edge parse and `git diff --check` passed locally. This is an audit view of the draft; it is not a whole-period VAT control reconciliation or an HMRC-ready return.
- After the VAT account change, the serial data-access regression passed outside the sandbox: 100 database/access cases and 10 boundary contracts, zero failures.
- A read-only GL VAT posting inventory now lists tax-labelled lines and lines on mapped or configured VAT accounts for overlapping accounting periods, with links to the selected draft. The PostgreSQL fixture checks an unlinked direct journal without a tax label, a tax-labelled line on the wrong nominal, a foreign-currency line excluded from GBP totals, bounded paging and cross-company denial. The focused fixture, client type check, Edge parse and `git diff --check` passed. The serial access regression also passed (100 database/access cases and 10 boundary contracts); the focused fixture was rerun after the final wrong-account test edit. This inventory does not establish VAT tax-point alignment or reconcile the full control balance.
- The inventory's GBP debit/credit totals now split draft-linked VAT accounts, unlinked VAT-account postings, and tax-labelled postings off VAT accounts. The fixture verifies each subtotal against mixed document and direct-journal lines, including exclusion of the USD line. The UI shows signed debit-minus-credit amounts with an explicit accounting-period scope; these are review figures, not a period approval gate.
- A read-time control bridge now shows unrounded source VAT due, VAT-account net credit, their exact four-decimal difference, linked VAT posting counts, and accounting-period coverage. The PostgreSQL fixture checks the £2 direct-journal difference and rejects an overlapping period that crosses the VAT boundary. The bridge remains diagnostic: a stored, locked control reconciliation and approval gate are still absent.
- For the control bridge change, the focused PostgreSQL fixture, client TypeScript check and `git diff --check` passed. The full serial access regression passed again: 101 database/access cases and 10 boundary contracts. No tenant migration or live UI journey was performed.
- An immutable VAT control-review table and manager-only RPC now record a dated snapshot after fresh calculation, all transaction sign-offs and a zero-difference, exact-scope GL check. The fixture rejects unsigned transactions and an unlinked journal, then records one review after the journal is removed; repeat review retains its first date. It verifies same-company read, foreign-company denial, immutable rows and audit. The Edge route and workspace expose review and readback. This is historical control evidence, not period approval or filing authority.
- The fixture also checks that a source-posting discrepancy prevents control review and leaves the prior dated snapshot unchanged. Client TypeScript, Edge parse, `git diff --check` and the serial access regression passed at that stage (101 database/access cases and 10 boundary contracts). No tenant deployment or live browser journey was performed.
- A new native posting migration charges nonrecoverable purchase VAT to the purchase cost nominal with a distinct posting label. A real PostgreSQL fixture executes the replacement function for a blocked purchase, recoverable purchase and blocked debit note, checking balanced journals, account destinations, reversal sign and idempotent retry. The VAT-period fixture verifies that an older input-VAT posting remains a source mismatch, the new cost posting matches, and nonrecoverable tax stays out of Box 4 while purchase net remains in Box 7. Historical corrections are still a separate governed workflow; this migration does not rewrite posted journals.
- Both focused PostgreSQL fixtures passed. The serial access regression passed with the new posting fixture included: 101 database/access cases and 10 boundary contracts. `git diff --check` passed. The new migration has not been applied to a tenant or checked in a live posting journey.
- The native source-to-ledger check now resolves the expected VAT nominal using the posting rule, rather than treating equal VAT amounts on any nominal as matched. The PostgreSQL fixture changes a source VAT line to a sales nominal, verifies a mismatch with readable account codes and a changed fingerprint, rejects sign-off against the stale draft, then restores the account. Changing the approved tax code's output nominal also invalidates the draft fingerprint until restored. This does not reconcile the whole-period VAT control account.
- After that nominal check, the focused PostgreSQL VAT fixture, client TypeScript check, finance Edge parse and `git diff --check` passed. The serial access regression passed outside the sandbox with 100 database/access cases and 10 boundary contracts, zero failures. No live tenant migration or browser verification has occurred.
- The focused VAT PostgreSQL fixture was rerun successfully with the actual nominal resolver migration loaded instead of a test stub. The full regression above ran before this fixture-only strengthening; no production SQL or client code changed afterward.
- A signed VAT evidence event can no longer be moved into a second period by changing its tax point. The PostgreSQL fixture checks the operator review rejection and, using rollback-only privileged setup, independently exercises direct decision, calculation-line and reconciliation guards. It confirms the original sign-off date survives and no second-period decision, line or sign-off remains. A formal correction-event workflow is still outstanding.
- The finance document's current VAT reconciliation date requires a sign-off against the latest stored calculation digest. It is a current-draft projection, not a live recalculation on every document read. A later tax-rule or calculation change can make current sign-off pending while historical sign-off remains; source posting changes are now blocked after sign-off.
- Document detail now exposes the original full VAT reconciliation date separately from current-draft sign-off status. A new migration keeps that historical date visible when a later calculation revision makes current sign-off pending. The focused PostgreSQL fixture passed for both states, including the retained source lock. Client TypeScript, `git diff --check`, and the full serial access regression passed (102 PostgreSQL/access cases and 10 boundary contracts). This local change has not been applied to a tenant or browser-verified.
- A server-only HMRC fraud-evidence assembler now combines browser device fields with verified Auth user/MFA evidence, fresh coherent ingress observations, and server product/licence/version values. It ignores browser-supplied network and vendor headers and fails closed on missing licence, version, stale network capture or inconsistent hops. Sixteen focused HMRC tests, Edge helper bundling and `git diff --check` passed. Its tests use synthetic observations only. No trusted tenant ingress adapter, actual licence source, HMRC Test API call, outbound VAT API route or live tenant check exists yet.
- A service-only, audited Vault access function now returns the current HMRC access token for a named VAT period operation, with tenant, legal-entity, registration, grantor, expiry and refresh-lease checks. It never returns the refresh token. Submission requires a reserved attempt and readback a claimed unresolved attempt. The focused PostgreSQL connection fixture passed for token rotation, lease denial, tenant/company denial, attempt-state denial, role grants and audit. The staged provisioning chain and full serial access regression passed afterward: 102 PostgreSQL/access cases and 10 boundary contracts; `git diff --check` passed. No Edge route calls this function yet, and no tenant migration was applied.
- A backend-only HMRC dispatch coordinator now connects fraud-evidence assembly, scoped token read, the committed one-way database claim, one protocol POST and durable receipt/uncertainty RPCs. Six injected-HTTP tests passed for success, preflight denial, lost claim response, changed payload hash, stale ingress, HTTP/network uncertainty and failed receipt persistence. Edge helper bundling and `git diff --check` passed. This is an offline contract: no Edge route, trusted ingress adapter, HMRC sandbox call or tenant deployment exists.
- The first sign-off also records one immutable, service-only evidence-to-period assignment. The PostgreSQL fixture checks its RLS, original actor and period, rejects direct changes and a second assignment, and confirms a repeated sign-off preserves the original date. This unique row prevents concurrent cross-period assignment independent of the read-before-write guards.
- After the assignment and cross-period guards, the focused fixture passed with assertions that each rejection came from the intended guard. The full serial access regression passed outside the sandbox with 100 database/access cases and 10 boundary contracts, zero failures; `git diff --check` passed. The last edit strengthened only the fixture's error assertions and was verified by rerunning that fixture.
- A local filing-projection migration now records an immutable, dated review of proposed whole-pound Boxes 6–9 alongside the original pence values, calculation digest, control fingerprint, reviewer, reason and versioned nearest-pound rule. Its preview shows source and proposed figures before the review action. PostgreSQL tests cover a £105.49 Box 7 becoming £105 without changing the source, role and company denial, idempotent review dates, immutability, and refusal to reuse a review after either a source posting or an unlinked VAT control posting changes. The focused VAT fixture, client TypeScript check, Edge parse and `git diff --check` passed. The full serial access regression passed after the final control binding: 101 PostgreSQL cases and 10 boundary contracts, zero failures. HMRC has not validated the rounding policy; no tenant migration, live browser journey or filing approval has occurred.
- A local review-lock migration now binds a period to the current calculation, control review and filed-value projection after revalidation, then moves it to `review_locked`. A separate dated unlock reason returns it to `draft`, even if the registration drifted and needs correction. The PostgreSQL fixture verifies manager-only locking and reopening, same-company read, foreign-company denial, immutable lock and unlock records, blocked recalculation/review while locked and denial of a direct unaudited reopen or reactivation. The focused fixture, client TypeScript check, Edge parse, production client build and `git diff --check` passed. The full serial access regression passed afterward: 101 PostgreSQL cases and 10 boundary contracts, zero failures. The build reported existing mixed-import and bundle-size warnings. This is a reversible review freeze, not HMRC obligation verification, statutory approval or submission. No tenant migration or live browser journey has occurred.
- Annual Accounting Scheme return calculation now uses normal UK VAT boxes with a separate `uk-annual-v1` digest; instalments do not reduce Box 5. An effective annual registration is required for period creation and evidence review, and the HMRC OAuth registration checks accept standard or annual while still rejecting cash. A PostgreSQL fixture posts and reviews an annual sale across a 12-month period, verifies the matched source and Boxes 1, 5 and 6, then rejects scheme drift and an unsupported cash period. The pure kernel and HMRC connection fixtures cover the same boundary. The serial access regression passed afterward: 101 PostgreSQL cases and 10 boundary contracts, zero failures; client TypeScript and `git diff --check` passed. Instalment, balancing-payment, due-date, obligation and submission workflows remain outstanding.
- A backend-only HMRC view-return helper now percent-encodes `#` period keys, requires the VAT API 1.0 `Accept` value and all fraud-header shapes, and checks every readback box against the proposed filing payload. A 404 is explicitly inconclusive. Sixteen focused HMRC protocol/return tests and `git diff --check` passed. This helper has no Edge route, durable attempt record, live HMRC call or submission authority.
- The VAT catalogue now references HMRC VAT API 1.0 for new tenants. An incremental migration corrects only the original 2.0 source URL in existing `gb-v1` and `gb-vat-mtd` rows, preserving tenant overrides and `foundation` readiness. Its disposable PostgreSQL fixture passed for fresh provisioning, correction and override preservation. The full serial access regression passed afterward: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. No tenant migration was applied.
- An immutable service-only HMRC obligation observation table now records an open period key, due date, correlation ID, environment and recording actor against the exact tenant project, connection, VAT registration, VRN and local period dates. The connection PostgreSQL fixture covers an accepted open response and rejects a different tenant, changed dates, fulfilled status and mutation. Seventeen focused HMRC tests passed, followed by the full serial access regression: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. This is not a live HMRC verification: the Edge obligations route, trusted fraud-header capture, final approval and dispatch gate remain absent. No tenant migration was applied.
- The same VAT calculation kernel now supports a read-only snapshot of a review-locked period. A locked-period freshness RPC compares the current source digest and boxes with the review lock and recomputes the whole-period VAT control fingerprint. The PostgreSQL fixture verifies unchanged evidence, manager-only access, no new calculation revision, a changed native VAT posting, an unlinked VAT control journal, registration drift and restoration after the test changes are removed. The focused fixture and full serial access regression passed: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. This is a preflight only; it does not approve or submit a VAT return.
- A service-only filing approval migration now stores immutable direct-business declaration confirmation against the review lock, reviewed boxes, fresh HMRC obligation observation and current authority. A dated revocation must precede unlocking. The PostgreSQL fixture rejects unconfirmed and agent declarations, a stale or foreign-tenant observation, a read-only colleague, duplicate active approvals and production use while the pack is `foundation`; it checks audit, immutability, revoke and reapproval. The focused fixture and full serial access regression passed at that stage: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. The operator route and UI were added later; no live HMRC observation or dispatch attempt has occurred, and no legal declaration has been confirmed by a real user.
- A service-only submission attempt now reserves the exact approved HMRC return body with a SHA-256 digest and one attempt per declaration. Claiming dispatch is one-way and must be committed before a future POST. A claimed attempt cannot be claimed again, cancelled as unsent, or followed by approval revocation or review unlock. The focused PostgreSQL fixture checks a changed payload, foreign tenant and read-only denial, reservation, body preservation, blocking, single claim and cancellation of a still unsent reservation. The full serial access regression passed: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. This is local state only: there is no sender, receipt, readback resolution, or live HMRC call.
- A local outcome migration adds a blocking `reconciliation_required` state and immutable HMRC 201 receipt evidence, including the form bundle number, processing date, correlation ID and receipt headers. The PostgreSQL fixture checks that a non-201 response cannot be recorded as accepted, an uncertain result cannot be cancelled, a valid 201 receipt becomes accepted, duplicate receipts are rejected, and accepted evidence cannot be changed or its approval revoked. The full serial access regression passed: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. There is still no submit Edge route, live HMRC response, or readback resolution of an uncertain result.
- A backend-only submission helper now sends one POST with the exact supplied body, VAT API 1.0 media type, bearer token and validated fraud headers. It parses a 201 receipt and classifies network failures, other HTTP results and malformed 201 responses as unresolved; it does not retry. Seventeen focused HMRC protocol and return tests passed. This helper is not wired to the durable claim or a live tenant.
- The HMRC view-return parser now preserves raw JSON numeric text and compares exact pennies, including maximum Box 1 amounts; it rejects duplicate or extra fields and fractional values outside the nine-box contract. An immutable, service-only readback check records 404 as inconclusive and mismatched 200 responses as unresolved; only an exact 200 match changes a claimed attempt to `accepted_readback`, separate from the 201 receipt state. The PostgreSQL fixture checks tenant and read-only denial, blocking after 404/mismatch, duplicate field rejection, one exact match, and later receipt capture. Eighteen focused HMRC protocol/return tests and the PostgreSQL fixture passed. The full serial access regression passed: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. No live HMRC readback or Edge route exists.
- A read-only filing-status RPC and finance Edge route now project obligation, approval, attempt, receipt and readback metadata to permitted colleagues, excluding OAuth tokens, raw responses, exact submission body and tenant project identity. The PostgreSQL fixture checks same-company viewer access and foreign-company denial. The VAT workspace renders sandbox evidence and unresolved outcomes separately. Client TypeScript, the finance Edge bundle parse and the production client build passed. The full serial access regression passed: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. The build reported the existing mixed-import and bundle-size warnings. No running-tenant browser journey or live HMRC status read has been verified.
- An explicit `outside_uk_service_box6` UK treatment now identifies reviewed service sales whose place of supply is outside the UK. The approved tax code must have a zero rate and the posted source must have zero VAT; signed sales and credits enter only Box 6. Generic `out_of_scope` remains unsupported. A setup selector exposes the distinct treatment for UK tax codes. The calculation digests advance to `uk-standard-v4` and `uk-annual-v2`. The focused PostgreSQL fixture passed for posted sale and credit source-to-ledger matches, Box 6 source lines, and rejection of UK VAT on the line; all nine pure kernel cases, client TypeScript and production client build passed. The full serial access regression passed: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. The build reported existing mixed-import and bundle-size warnings. This is a UK return classification, not a determination of foreign tax liability. No tenant migration or browser journey has occurred.
- A new local migration freezes a VAT-reconciled document's financial fields, lines, job links and native journal batch/lines at the database boundary. It allows the invoice's outstanding balance and provider delivery state to progress without changing the VAT source. The reconciliation path locks source documents before its freshness calculation, and that calculation now checks posted line amounts against immutable evidence. Document detail and VAT account views report the historical source lock independently of whether the latest calculation still shows a current reconciliation date. The PostgreSQL fixture passed for direct edit, insert and delete denial; unchanged sign-off dates; and allowed settlement/delivery updates. Client TypeScript and production build passed. The full serial access regression passed: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. The build reported existing mixed-import and bundle-size warnings. No tenant migration or browser journey has occurred.
- The source lock now follows a posting or job link's referenced document line even when the new row names an unrelated batch or document. The focused PostgreSQL fixture rejects both indirect insertion attempts with the VAT lock error. The full serial access regression passed afterward: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. The change remains local and has not been applied to a tenant.
- A new local migration links each exact billing-party correction reversal line to its original immutable VAT evidence line. The PostgreSQL fixture posts an original and reversal, rejects a changed reversal amount, retains an ordinary credit as independent evidence, and rejects a forged reversal pointer. The full serial access regression passed afterward: 101 PostgreSQL cases and 10 boundary contracts, zero failures; `git diff --check` passed. It does not classify a correction as a previously filed return error. No tenant migration or live correction journey has occurred.
- The UK VAT workspace now shows HMRC connection and expiry status, starts sandbox consent on HMRC's exact authorisation host, and requests server-side access-token renewal. The OAuth callback returns to `/finance/vat`, where success or failure is shown without exposing tokens or raw provider messages. Client TypeScript, the production build, Edge bundle parsing, 12 focused HMRC database/protocol tests and `git diff --check` passed. The build reported the existing bundle-size warning. A local browser visit to `/finance/vat` correctly rendered sign-in for an unauthenticated visitor with no page errors; an authenticated VAT panel, live sandbox consent, provider redirect, tenant persistence and fraud-header collection remain unverified.
- The VAT account now projects a linked reversal's original evidence ID, document number, type and first VAT sign-off date through the existing bounded, permissioned account read. The workspace links to the original finance document only for colleagues with that ledger's view permission. The PostgreSQL fixture verifies linked reversal and ordinary-credit rows, same-company viewer readback, foreign-company denial, and no direct browser-role execution of either account function. Client TypeScript and production build passed. The full serial access regression passed: 101 PostgreSQL cases and 10 boundary contracts, zero failures; the focused fixture passed again after the final browser-role assertions, and `git diff --check` passed. The build reported the existing bundle-size warning. A live authenticated VAT account journey remains unverified.
- A new PostgreSQL regression loads the 7.2 MB provisioning schema in its documented order: core schema, the existing general-ledger journal migration, then the finance patch tail. It applies the current local VAT migration sequence in filename order and finds all five VAT source-lock triggers and the reconciliation timestamp column. The harness supplies local Supabase `auth` and extension stubs; it does not alter the schema or invent the journal table. The focused test passed and the full serial access regression passed outside the sandbox: 102 PostgreSQL/access cases and 10 boundary contracts, zero failures; `git diff --check` passed. This verifies SQL compatibility with the staged baseline, but not the full hosted Supabase provisioning workflow, reference data, storage, tenant data, or live HMRC calls.
- HMRC's VAT service guide says the obligation period key is for software use and should not be shown to businesses or agents. The local filing-status projection now omits it from obligation, approval and attempt readbacks, while the protected server records retain it for HMRC matching. The VAT workspace no longer displays the key. The PostgreSQL fixture checks same-company status access and that no key leaks through the response. The focused fixture, client production build and full serial access regression passed afterward: 102 PostgreSQL/access cases and 10 boundary contracts, zero failures; `git diff --check` passed. The final explanatory-copy edit followed those checks. There is no live authenticated VAT journey yet. Official source: https://developer.service.hmrc.gov.uk/guides/vat-mtd-end-to-end-service-guide/documentation/obligations.html
- A local browser collector now retains a UUID device ID and formats the browser-reported user agent, current screen, window dimensions and UTC offset for HMRC's web-app-via-server headers. It fails closed when source data or persistent storage is unavailable; its two focused tests check device persistence, timezones, failure states and compatibility with the backend header validator. Client TypeScript passed and `git diff --check` passed. This collector is not called by the VAT workspace or any HMRC Edge route. It cannot supply or verify the public source port, public IP, MFA event, intermediary hops or vendor evidence; no HMRC VAT API call was enabled. Official source: https://developer.service.hmrc.gov.uk/guides/fraud-prevention/connection-method/web-app-via-server/
- The backend obligation GET now returns HMRC's validated 36-character `X-CorrelationId` beside the parsed obligations, so the existing immutable observation RPC can retain the actual response reference. A successful response without a valid reference fails closed. The 13 focused browser and HMRC protocol tests passed and `git diff --check` passed. This does not create an Edge route or prove an HMRC sandbox response; fraud-header provenance still blocks an outbound call. Official contract: https://github.com/hmrc/vat-api/blob/main/resources/public/api/conf/1.0/application.yaml
- The local VAT workspace now shows all nine values from the immutable review lock's filing projection alongside HMRC's exact business declaration. Confirmation requires an unchecked-by-default box, Compliance Manage permission, a fresh recorded obligation, matching review lock, and no active approval or live submission attempt; an audited, cancelled unsent reservation does not block a new declaration. The finance Edge route derives the tenant project identity from its own `SUPABASE_URL`, hard-codes the business declaration code, and calls the audited approval RPC; a second route records reasoned revocation before dispatch. A follow-up migration makes filing status prefer the newest HMRC observation after revocation, while retaining the linked observation during an active approval. The focused PostgreSQL VAT and staged-provisioning fixtures, Edge bundle parse, client TypeScript check, route contract and full access regression passed locally: 102 PostgreSQL/access and 10 boundary cases, zero failures. No authenticated browser approval journey or HMRC observation exists yet; the action remains disabled until a trusted HMRC check can be recorded. Official declaration: https://developer.service.hmrc.gov.uk/guides/vat-mtd-end-to-end-service-guide/documentation/obligations.html
- Transaction sign-off now closes the same-period decision-revision path as well as the posted source lock. The PostgreSQL fixture rejects a later reviewed decision and a direct insert, while preserving the original reconciliation timestamp and allowing operational settlement/delivery updates. A new UK VAT registration setup RPC and operator panel let a manager enter an invoice-basis standard or annual registration for a new GB legal entity or complete a `not_configured` placeholder. The server derives the active obligation, requires explicit invoice-basis confirmation, records actor and audit, sets only `configured`, and rejects read-only/foreign callers, unsupported cash scheme and overwrite attempts. The fixture prepares an annual draft period from a newly configured registration. Focused PostgreSQL/route tests, staged tenant schema, Edge bundle parse, client production build and the final full serial access regression passed: 102 PostgreSQL/access and 10 boundary cases, zero failures; `git diff --check` passed. No authenticated browser journey or tenant deployment has occurred.
- A later local migration permits prospective, effective-dated UK VAT registration changes without mutating the old VAT number or scheme. It rejects overlapping terms, no-op revisions, non-GBP entities, read-only/foreign actors, unconfirmed invoice accounting, prepared periods that cross the change date, and old periods without an accepted **production** return. A new period selects the registration that covers its entire date range; a closed registration cannot start a historical draft. Immediate changes revoke old HMRC authority, and read-only status reports an expired registration's connection as requiring consent even before token refresh. The operator page shows current and scheduled terms and records the reason. The focused PostgreSQL fixture, staged tenant schema, route contract, client build and Edge bundle parse passed locally. A live tenant, HMRC sandbox/production consent and authenticated browser journey remain unverified.
- Registration effectiveness, HMRC consent and token renewal checks, and the production return end-date gate now use the UK civil date explicitly. This keeps those decisions stable when a tenant or worker database session uses another time zone, without changing VAT evidence fingerprint serialization. PostgreSQL fixtures exercise an intentionally different caller date for immediate registration changes and HMRC consent; the focused period and connection fixtures and staged tenant schema passed. The full serial regression passed after the UK date adjustment: 102 database/access cases and 10 boundary contracts, zero failures. The adjustment has not yet been applied to a tenant or tested against HMRC.
- An unchanged review lock was reproduced as stale when the same VAT period was read from a different PostgreSQL session time zone. The registration update timestamp in the calculation snapshot now uses a canonical UTC representation before fingerprinting, and the standard and annual calculation versions advance to `uk-standard-v5` and `uk-annual-v3`. The PostgreSQL fixture checks review-lock freshness after switching to Honolulu time while retaining earlier source-change rejection cases. The focused VAT fixture, staged tenant schema and full serial access regression passed: 102 database/access cases and 10 boundary contracts, zero failures. Existing stored calculations created with older versions would need an explicit review and lock refresh if this change is ever applied after live VAT use.
- Direct service-role inserts and updates to VAT reconciliation and first-period-assignment rows are now denied; the permissioned reconciliation RPC remains the audited writer. The existing insert trigger overwrites any caller-supplied reconciliation timestamp with the database time. The PostgreSQL fixture checks a privileged forged timestamp in a rollback-only probe, verifies the final sign-off and assignment share the date and reviewer, and executes direct insert attempts as the service role to confirm denial. The focused VAT fixture, staged tenant schema and full serial access regression passed: 102 database/access cases and 10 boundary contracts, zero failures. No tenant has received this change.
- The same direct-write restriction now covers all `FIN_IndirectTax*` period, evidence, decision, calculation, reconciliation, control-review, filing-projection and review-lock tables. Protected Edge routes already use server functions for writes and read the tables only where needed. A PostgreSQL fixture checks zero service-role INSERT/UPDATE/DELETE grants, directly attempts inserts as that role, and proves the service role can still create a period through the permissioned SECURITY DEFINER RPC in a rollback-only probe. The focused VAT fixture, staged tenant schema and full serial access regression passed: 102 database/access cases and 10 boundary contracts, zero failures. No tenant has received this change.
- A fresh focused PostgreSQL run again passed the transaction sign-off date and source-lock fixture. The [HMRC fraud-evidence gate](../architecture/hmrc-fraud-evidence.md) now identifies the provenance of each required web-app-via-server header and drafts an unsent support question about the missing public TCP source port and possible absent device licence. This is a documentation and readiness step only: no sandbox request, HMRC exception or tenant deployment has occurred.
- A server-only HMRC TOTP evidence helper now verifies a Supabase JWT's `aal2` identity and recent timestamped `amr` TOTP event, reads that user's verified factors through the Auth admin API, and emits a stable tenant-specific hash only when there is exactly one TOTP factor. Two focused tests cover stable formatting, tenant separation, another user, stale prompts, ambiguous factors and `aal1`; both pass. The helper is not wired to an outbound VAT call, and the public TCP source port and ingress chain remain unresolved.
- A new local migration adds an immutable, audited link from an ordinary posted UK VAT credit line to its original invoice evidence without editing either posting or treating it as a filed-return error. The protected RPC checks manager and same-company access, document direction, party, currency and cumulative net/VAT credit limits; the VAT account readback and inline audit trail show the separate correction relationship. The focused PostgreSQL fixture checks permission denial, direct service-role write denial, exact reversal exclusion, overcredit rejection, retry date, immutability, audit and readback. Staged tenant schema, client TypeScript and the full serial access regression passed at that stage: 102 database/access cases and 10 boundary contracts, zero failures; `git diff --check` passed. An operator link action was added subsequently; a live tenant browser journey remains outstanding.
- The ordinary-credit workflow now has an operator search and reasoned link action in the VAT account trail, backed by Compliance Manage Edge routes and a bounded, permissioned invoice-line search RPC. Candidate rows include source currency, line number, remaining net/VAT and original sign-off date. A shared advisory lock and reciprocal guards prevent combining a full exact reversal with partial credit links, including concurrent attempts. The focused PostgreSQL fixture covers candidate selection, read-only denial, overcredit removal from results and both conflict directions; client TypeScript and the local Edge bundle parse passed. The full serial access regression passed after these changes: 102 database/access cases and 10 boundary contracts, zero failures. A live authenticated browser journey and tenant deployment remain unverified.
- A server-only `Gov-Client-User-IDs` helper now cross-checks a verified JWT subject and email with the current confirmed Supabase Auth user, then percent-encodes the internal user ID and login email. It ignores editable user metadata and fails closed on a mismatched subject, changed email, unconfirmed email or non-user role. The focused HMRC protocol and identity tests passed: 14 cases, zero failures; helper bundle parse and `git diff --check` passed. The helper is not wired to a VAT API route, and the public source port and ingress chain remain unresolved.
- HMRC OAuth state and connection references now allow service-role reads but no direct inserts or updates; the guarded consent and token-refresh functions remain the writers. The internal reauthorisation helper is no longer directly executable as the service role. PostgreSQL checks direct denial, a rollback-only service-role OAuth begin through the public function, token secrecy, single-use state, refresh rotation and staged tenant schema. The focused HMRC and staged-schema fixtures passed, followed by the full serial access regression: 102 database/access cases and 10 boundary contracts, zero failures. No live HMRC call or tenant migration was performed.
- A UK VAT draft now fails closed when a posted supplier invoice has reviewed recoverable input VAT and remains unpaid at least six months after the later of its tax point and due date. The check reconstructs payment at the VAT period end from posted supplier allocations, so a later payment cannot erase an earlier risk. The focused PostgreSQL fixture covers due-date deferral, partial and full payment, later payment, and calculation denial. The staged tenant schema and full serial access regression passed locally: 102 database/access and 10 boundary cases, zero failures; the Dexter Edge bundle parsed and `git diff --check` passed. This is an exception gate, not a clawback calculation or adjustment; accountant review and the actual adjustment workflow remain outstanding. No tenant migration was applied.
- The supplier input VAT gate now has a bounded period readback and a VAT workspace panel. It lists the affected invoice, due date, first possible six-month date and amount unpaid at period end, with a payable-document link only for colleagues who can view payables. The protected Edge route requires Compliance View and legal-entity access; the PostgreSQL fixture checks a same-company viewer, foreign actor, unknown period, partial payment and later payment. Focused PostgreSQL and staged tenant tests, client TypeScript, production build, Edge bundle parse and the full serial access regression passed locally: 102 database/access and 10 boundary cases, zero failures. The build reported existing mixed-import and bundle-size warnings. The panel is diagnostic only and has not been exercised in an authenticated browser or deployed tenant.

## Verification gap

- The intended product offer is Standard Accounting by default and Cash Accounting as the secondary option. The local VAT screen now shows Cash as in development and no longer offers new Annual Accounting setup, while existing annual registrations remain legible. Standard setup can suggest a three-month draft from the period end on the last or next return; the operator explicitly applies those dates, and HMRC obligation dates remain authoritative. The quarter helper passes year-boundary and invalid-date checks, and client TypeScript passes. Cash payment-event extraction, partial-payment apportionment, scheme transitions, approval and live HMRC testing remain outstanding.
- A backend-only Cash Accounting arithmetic helper now covers HMRC Notice 731's £780 part-payment example (£130 VAT) and £2,730 split over an older £1,200 invoice plus £1,530 against a mixed-rate invoice (£180 VAT on the latter). Focused tests also cover supplier input VAT, cumulative rounding to full settlement, invalid source amounts and dates. The helper is not wired to posted cash allocations, VAT periods or the UI; Cash remains disabled.
- HMRC Notice 700/45's previous-return error routing is now defined in a backend-only exact-penny helper and focused tests: net errors up to £10,000 may be adjusted on the current return; £10,000–£50,000 also needs the 1% current-Box-6 test; larger or deliberate errors require separate notification. The helper does not itself record errors, perform the four-year time-limit check, adjust a current return, or submit an HMRC error correction notification. Earlier-than-previous-return age alone is not a separate-notification rule.
- A local follow-up adds immutable, dated prior-period error intake with source reference, original and discovery periods, signed GBP VAT, tax side and explicit conduct category (including undetermined). A manager-only Edge route and VAT screen record it; same-company compliance viewers can read it, while foreign and read-only writes are denied. Duplicate source keys are idempotent only for identical evidence; changed amounts are rejected. The PostgreSQL fixture and staged tenant migration chain passed after the conduct-category revision. Intake has no effect on a return and does not submit an HMRC notification. Dexter chat and Watching for you explicitly say this intake is unsupported pending a safe read and event adapter. The pure threshold helper is not yet the reviewed assessment or wired to VAT-account posting.
- A manager-only preview now aggregates the actual immutable intake, uses a matching reviewed Box 6 projection only for the £10,000–£50,000 branch, and rechecks the current source digest before using it. The fixture confirms £12,500 without reviewed Box 6 stays undecided, that voluntary Method 2 previews as a separate notification, and that read-only users cannot request a method preview. Conduct and time-limit review warnings are visible in the VAT screen. Focused PostgreSQL and staged tenant schema tests passed. The method has not been approved, posted to the VAT account, included on a return, or sent to HMRC.
- Manager-only conduct decisions are now append-only, dated and audited, with revisions retained. The VAT screen shows the effective decision and its dated reason history, and lets managers review it while the discovery period is draft. The PostgreSQL fixture verifies same-company read-only and foreign-user denial, idempotence, immutable review rows, retained revision history, deliberate Method 2 routing, and a later revision back to careless. The method preview still requires a separate assessment of time limits and original filing evidence before any correction can affect a return.
- The method preview also flags a single error for immediate Method 2 review when it exceeds £50,000, or exceeds the 1% test using a current reviewed Box 6. The operator screen warns against waiting for the period end. A PostgreSQL case proves the unconditional £50,000 alert and the reviewed-Box-6 alert. The product does not dispatch the separate notification.
- A local follow-up adds immutable operator-entered evidence for a separate HMRC error notification already made online or by letter. An explicit confirmation, notified date, evidence reference and selected intake IDs are required; each intake can be linked only once. Same-company read-only users can inspect the evidence, while read-only and foreign writes are denied. The PostgreSQL fixture verifies idempotence, duplicate rejection and audit. This is not an HMRC acknowledgement, an approved correction, or a current-return adjustment. No live notification or tenant deployment was performed.
- The protected method preview now switches to a notification-history state once any intake has been linked to external notification evidence. A complete set shows all items linked; a later unlinked discovery requires review rather than being offered as an automatic current-return adjustment. The fixture tests both transitions. The older raw threshold RPC is no longer executable by the service role; the Edge route calls the notification-aware RPC.
- The Cash Accounting foundation now freezes posted cash source dates, amounts and allocation links through database triggers while permitting approved-to-submitted provider delivery. Focused PostgreSQL tests reject a backdated posted receipt, a changed amount, allocation edits, late allocation insertion and deletion; the staged tenant migration chain passes. These locks do not make Cash Accounting selectable. A reviewed payment-kind date, allocation-derived VAT event, scheme entry/exit, exclusions and cash return reconciliation are still missing.

- A backend-only exact-decimal kernel now calculates a six-month unpaid-input-VAT repayment and later restoration from supplied source-currency gross/payment amounts and previously repaid GBP VAT. The focused tests cover HMRC Notice 700/18's £116.66 and £58.33 examples, a later part payment, full payment, due-date timing, month ends and invalid inputs; all passed, as did the helper bundle parse and `git diff --check`. It is not wired to the VAT draft, a posting or a filing.
- A service-only obligation context and backend-only coordinator now bind one HMRC GET to server-owned dates, current authority and complete fraud evidence. The coordinator records exactly one matching open obligation and returns no period key; closed, missing and duplicate matches, foreign scope, lost writes and network errors fail closed. Focused fake-HTTP and PostgreSQL tests, staged tenant schema provisioning, the helper bundle parse and the full serial access regression passed locally: 102 database/access cases and 10 boundary contracts. Two fixture dates were made stable across the UK/UTC midnight boundary after the first full run exposed failures in VAT registration scheduling and an unrelated CRM task date. There is still no outbound tenant route or HMRC sandbox request.
- A local service-only readback context now releases the HMRC period key only for a claimed unresolved attempt in the same tenant, entity, registration, VRN and environment. A backend-only coordinator performs one authenticated GET with assembled fraud evidence and records raw 200 or 404 evidence through the database's exact comparison; mismatched figures stay blocking. Focused fake-HTTP and PostgreSQL tests, staged schema provisioning, helper bundle parse, `git diff --check`, and the full serial access regression passed. No tenant route or HMRC sandbox call was made.
- A new local service-only UK input-VAT clawback source preflight derives the original Box 4 line from a signed calculation and production-accepted return with a receipt or matched readback. It returns source-currency gross, original GBP input VAT, six-month date, dated posted supplier allocations and a source fingerprint; credited, reversed, unsigned and unfiled sources fail closed. The focused PostgreSQL fixture verifies manager access, read-only and foreign-company denial, missing-receipt denial, and a £120 invoice with £20 originally claimed, £30 paid before the six-month date and £20 later paid in the target period. The staged schema test and full serial access regression passed: 102 PostgreSQL/access and 10 boundary cases, zero failures. This read-only preflight is not an approved VAT adjustment, does not post to the control account or Box 4, and is not connected to an HMRC route or live tenant.
- A local follow-up migration makes the unpaid-input-VAT period gate require both a posted supplier cash date and the allocation's UK civil date to be on or before the VAT period end. The focused PostgreSQL fixture passes with an allocation entered after the period despite an earlier cash date, then confirms partial/full timely payment and both sides of the British Summer Time midnight boundary. The staged tenant schema test passed. An adjustment and restoration posting workflow remains outstanding.
- The local clawback source snapshot now fingerprints only allocations effective by the selected VAT period end, together with its period dates, invoice date, due date and source amounts. A focused PostgreSQL case adds a later payment and confirms the prior period's fingerprint, payment total and payment list remain unchanged; the staged schema test passed. HMRC's web-app-via-server guidance still requires the originating public TCP port. Supabase Edge Functions run behind a gateway, and the documented `x-forwarded-port` example is the HTTPS listener port; no trusted ingress adapter or outbound HMRC VAT route has been enabled. Official sources: https://developer.service.hmrc.gov.uk/guides/fraud-prevention/connection-method/web-app-via-server/ and https://supabase.com/docs/guides/functions/logging
- A local service-only preparation persists an immutable, audited first-period input VAT repayment proposal from the verified original production claim and dated supplier payments. The fixture checks £20 originally claimed on a £120 invoice, £30 paid by the six-month date, another £20 paid before period end, £15.00 initially repayable, £3.34 restored in the same period and a net negative £11.66 Box 4 delta. Full settlement later in the same period still records both £15.00 entries even though the net delta is zero. The proposal remains separate from posting and filing. The earlier focused, staged and full regression results applied before this timing correction; the corrected rule needs its own full regression.
- A local service-only repayment review now binds an immutable proposal to an active non-control offset nominal and rechecks the original filing and period-end payments before recording an audited, dated decision. The focused PostgreSQL fixture covers a changed allocation making the proposal stale, read-only and foreign-company denial, invalid control account, idempotent review, account change requiring revocation, audited revocation and replacement review. The staged tenant schema test, Dexter Edge bundle parse and full serial access regression passed: 102 database/access and 10 boundary cases, zero failures. This is a review for later posting; no journal, VAT adjustment evidence or Box 4 line is created by it.
- A local payment-date review for posted UK cash records now captures the method event date, cheque date where applicable, derived VAT payment date, evidence reference, actor, reason, source fingerprint and immutable revision history. Cheques use the later of the received/sent date and cheque date. The operator VAT page can review and read these dates, while the scoped Edge routes and database functions enforce company access and management permission. Cash Accounting remains disabled: reviewed dates do not yet create VAT events or change a return. The client build, Edge bundle parse, focused route contract, PostgreSQL fixture and staged schema passed locally; the full serial access regression passed with 102 database/access and 10 boundary cases, zero failures. No authenticated tenant browser review or HMRC sandbox test has occurred. HMRC method guidance: https://www.gov.uk/guidance/vat-cash-accounting-scheme-notice-731
- A read-only Cash Accounting source snapshot now extracts actual posted cash allocations for invoices paid in a selected period, including earlier reviewed payments to the same invoices and their latest reviewed VAT lines. A server preview fails closed on missing payment-date reviews, partial extraction, changed source fingerprints, incomplete allocations, unsupported credits or advances, foreign/non-GBP documents, and incoherent posted line evidence. It applies cumulative part-payment arithmetic and returns traceable candidate source amounts for Boxes 1, 4, 6 and 7, without creating VAT events or a return. Focused pure tests cover prior settlement and unsupported sources; the PostgreSQL fixture covers same-company read, foreign denial, containment of a foreign linked document, the reviewed cheque date and linked document lines. The staged tenant schema, Edge bundle parse, client build/TypeScript check and full serial access regression passed locally: 102 database/access and 10 boundary cases, zero failures. This is still a preview, with Cash Accounting registration and filing disabled; no authenticated browser or live tenant has exercised it.
- The cash source preview now checks whether each invoice line was signed in a Standard or Annual period. It excludes a payment only when every line is tied through the filed calculation and review lock to an accepted **production** HMRC attempt; the exclusion remains visible with allocation and acceptance time. A signed but unaccepted return, or mixed accounting across one invoice, blocks the preview instead of silently omitting or double counting VAT. Invoices with no evidenced due date or with payment due more than six months after issue also block, matching Notice 731's excluded-supply rule. Focused pure tests cover accepted exclusion, unaccepted denial and long-credit denial; a rollback-only PostgreSQL probe checks both the unaccepted and accepted-production joins. Staged schema, Edge bundle parse, client TypeScript and the full serial access regression passed locally: 102 database/access and 10 boundary cases, zero failures. This remains local source preparation, not a scheme transition approval or Cash Accounting return. HMRC guidance: https://www.gov.uk/guidance/vat-cash-accounting-scheme-notice-731
- Cash Accounting now has an immutable, audited payment-event projection for reviewed posted GBP invoice allocations. The manager route recomputes the read-only preview; the write function rereads the same source in its own transaction, validates each event's identifiers and cumulative four-decimal net/VAT apportionment, checks complete allocation-line coverage and candidate Box 1/4/6/7 totals, then stores source-bound event lines. Repeating the same projection is idempotent; a later payment-date review makes the old source digest visibly stale. A rollback-only PostgreSQL case covers a £60 part receipt against a £120 invoice (£50 net and £10 VAT), tampering, same-company read, read-only/foreign write denial, and stale-source rejection. The staged tenant schema, Edge parse, client TypeScript and focused contract tests passed locally. The full serial access regression passed with 102 PostgreSQL/access and 10 boundary cases, zero failures. These projections do not create Cash Accounting VAT evidence, alter the nine-box return, approve a filing, or enable Cash scheme selection; credits, advances, foreign currency, scheme entry/exit and filing controls remain outstanding. Dexter and Watching explicitly direct operators to the VAT screen because they have no safe projection adapter yet.
- The final VAT declaration and submission reservation/claim now reject a discovery period with any prior-return error intake that has no linked external Method 2 notification evidence. The gate is in database triggers, so direct service-role writes cannot bypass it. A later error added after a reservation blocks dispatch; recording a separate notification for that error clears the gate. The operator screen states this explicitly. A rollback-only PostgreSQL case covers approval denial, reservation denial, dispatch denial and clearance after notification. The staged migration chain and client build pass; the full serial access regression passed with 102 PostgreSQL/access and 10 boundary cases, zero failures. Method 1 remains a preview only until its reviewed VAT-account posting and return adjustment are implemented. HMRC permits Method 2 notification below the Method 1 limits, but recording a notification in Multideck does not send or verify it.
- A local time-limit assessment now records immutable, audited review revisions per prior-return error. It derives the ordinary four-year deadline from the original return due date for underclaimed input VAT, and from the original period end for output errors and overclaimed input VAT. The source reference, original return reference, evidence reference, reviewer, assessment date and in-time result are readable in the VAT screen. The PostgreSQL fixture covers read-only and foreign denial, conflicting tax-side/sign categories, required due date, expired input claim, immutable review and audit. The staged tenant migration chain, client build and full serial access regression passed locally: 102 PostgreSQL/access and 10 boundary cases, zero failures. This assessment does not adjust a VAT account or return, and the ordinary deadline must be checked again at actual correction. Deliberate and tax-point exceptions need specialist review. Dexter chat and Watching remain an explicit unsupported exception for this assessment because no scoped adapter or event exists yet.
- A local Method 1 planning step now snapshots every discovered error in a draft Standard period, the latest conduct and time-limit reviews, each Box 6/7 net-value delta, evidence reference, and an active non-control offset account. It evaluates the aggregate signed VAT error against £10,000 or the conditional £50,000/1%-of-final-filed-Box-6 limit. The plan is immutable, audited, idempotent, and visible in the VAT screen; a changed base source digest or error count is shown as stale. The PostgreSQL fixture proves manager/read-only/foreign boundaries, complete-set and duplicate enforcement, confirmation, a successful £100 case, rejection below the 1% ceiling and acceptance at the conditional ceiling. Focused PostgreSQL, staged tenant schema, Edge bundle parse, client build and full serial access regression passed locally: 102 PostgreSQL/access and 10 boundary cases, zero failures. This is a reviewed plan only: no native posting, adjustment evidence, nine-box change, gate clearance or HMRC notification occurs. Dexter chat and Watching remain explicitly unsupported for this planning record pending a scoped adapter and event.
- A subsequent local Method 1 posting step consumes the latest reviewed plan after the draft Standard period ends. It rechecks the complete intake set, source digest, latest conduct and four-year deadline reviews, offset accounts and final Box 6 threshold; posts a balanced native journal; creates immutable adjustment evidence and decisions; and exposes the batch and per-error links to permitted colleagues. Recalculation carries posted output corrections into Boxes 1/6 and input corrections into Boxes 4/7, with tax lines included in the VAT-control bridge. The filing gate clears only after every posted correction is reconciled into the active review lock's calculation. The rollback-only PostgreSQL fixture proves manager, read-only and foreign posting boundaries, duplicate denial, balanced output posting, nine-box effect, VAT-control linkage and the lock-dependent gate. The focused fixture and staged tenant chain passed; the Edge bundle parsed and the client built. The serial access regression passed outside the sandbox (102 PostgreSQL/access and 10 boundary checks, zero failures); the initial sandbox attempt could not allocate PostgreSQL shared memory. This has not been deployed or checked against a live tenant. The fixture exercises a focused ledger schema, so an end-to-end run with a real tenant's GL configuration and an accountant's original filing evidence remains required. Dexter chat and Watching remain explicitly unsupported for these posting records pending a scoped adapter and event.
- Cash Accounting projection integrity now has a service-only, company-scoped database check. It re-reads the current payment and invoice source, recomputes cumulative part-payment event amounts, verifies complete allocation-line and Standard-exclusion coverage, and compares Boxes 1/4/6/7 against the immutable projection. It returns a fingerprint for a future Cash return calculator and rejects stale source revisions. The focused PostgreSQL test covers the valid £60 part receipt, same-company read, foreign denial, altered event amount and a changed payment review; staged tenant migration application passed. The serial access regression passed outside the sandbox (102 PostgreSQL/access and 10 boundary checks, zero failures). The projection still has no VAT-return effect, and Cash filing remains disabled while credits, entry/exit and ledger timing controls are incomplete.
- A local supplier input VAT repayment path now takes an active accountant review through a dated schedule and service-only native posting. The posting writes one balanced VAT-control/offset pair and immutable VAT evidence for the six-month repayment and each later supplier payment effective in that same Standard period. Calculation revalidates the current payment cutoff, review, schedule and journal, carries the signed events into Box 4 without altering Box 7, and links both VAT-control tax lines to the control bridge. A later payment after that period blocks calculation in its new period until a follow-on restoration workflow is implemented. The focused PostgreSQL fixture verifies £15 repayment and £3.34 same-period restoration, Box 4 at -£11.66, Box 5 at £11.66, two linked native control lines, access denial, audit and duplicate/revocation guards. Staged tenant migration application passed. The full serial access regression passed with 102 PostgreSQL/access and 10 boundary checks, zero failures. The posting is still backend-only, with no operator Edge route, and has not been deployed or checked against a live ledger.
- A local multi-period supplier input VAT restoration chain now verifies the first repayment and each later posting against active review locks, source-cutoff payments and native journals. It dates payments using the later of cash date and UK allocation date, fingerprints only payments effective by the selected period end, and rejects a payment omitted from an intervening return. An immutable accountant review binds the source and offset nominal. The service-only confirmed posting writes balanced native journal lines, VAT evidence and decisions for positive Box 4 restorations; a zero-penny effect receives an audited no-journal posting that advances the unpaid balance. The Standard calculation revalidates every current-period posting, includes its dated events in Box 4 only, and links its tax lines to the VAT control bridge. The focused PostgreSQL fixture follows £15 original repayment, £3.34 same-period restoration, £3.33 October restoration, £1.67 January restoration and a later 1p zero-effect payment. It covers read-only/foreign denial, repeat review/posting prevention, source and journal tampering, control linkage and previous-lock dependency. Staged tenant migration application and the full serial access regression passed locally (102 PostgreSQL/access and 10 boundary checks, zero failures); Dexter's explicit unsupported wording bundled successfully. This is local backend work, not an operator-accessible or deployed filing flow.

Sandbox attempts sometimes could not initialise disposable PostgreSQL databases (`Operation not permitted` from System V shared memory). The failed disposable runs left unattached test segments, which were inspected and removed. One outside-sandbox run then exhausted shared-memory slots; the failed run's unattached test segments were removed and the final serial run passed. CI and target-tenant preflight are still required before deployment.

No live tenant preflight, historical backfill, accountant review, HMRC sandbox consent, fraud-header verification, production approval or live filing has occurred. Refresh rotation and obligation readback have local backend implementations but have not been tested against HMRC or exposed through a tenant VAT API route.
