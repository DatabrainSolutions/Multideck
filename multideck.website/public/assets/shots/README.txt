Screenshots go here.

Drop a PNG or WebP in this folder, then add `src: "/assets/shots/<file>"` to the
matching shot() call in src/pages/. Each placeholder states the screen it wants
and what should be on it. Aspect ratios are locked by the slot, so a screenshot
of roughly the right shape will crop from the top and never shift the layout.
